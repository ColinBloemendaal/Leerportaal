# Leerportaal — Build Plan

Work top to bottom. Do not start a phase until the previous one is fully checked.
Read `CLAUDE.md` before starting any task.

Legend: `[ ]` todo · `[x]` done · `[~]` in progress · `[!]` blocked (add a note)

---

## Phase 0 — Foundation

- [x] `composer create-project laravel/laravel` — PHP 8.3, Laravel 12
- [x] Git init, `main` protected, branch naming per CLAUDE.md §10 — branch protection skipped by explicit human decision (solo work, direct commits to `main` allowed for now)
- [x] Install Inertia (server + client) + Vue 3 + TypeScript + Vite
- [x] Install Bootstrap 5.3 + Sass. Create `resources/sass/_variables.scss` and `app.scss`
- [x] Confirm no Tailwind anywhere in the project (remove Laravel's default)
- [x] Install Pint, Larastan (level 8), Pest, Pest coverage plugin — coverage driver (Xdebug/PCOV) not installed locally on this machine; wired into CI instead
- [x] Install ESLint, Prettier, `vue-tsc`
- [x] Add `composer check` / `composer fix` / `npm run lint` / `npm run types` scripts
- [x] GitHub Actions CI: Pint, Larastan, Pest with 85% coverage gate, ESLint, vue-tsc
- [x] Configure `.env.example` with every key the project needs
- [x] `Model::preventLazyLoading()` + `preventSilentlyDiscardingAttributes()` in non-production
- [x] Create the layer directories from CLAUDE.md §3 (`Actions`, `Contracts`, `DataTransferObjects`, `Repositories/Eloquent`, `Services`, `Http/Resources`, `Enums`, `Exceptions`)
- [x] `RepositoryServiceProvider` with interface → implementation binding convention
- [x] Base classes/interfaces: `QuestionType`, `PaymentGateway` stub, base repository interface pattern
- [x] `tests/Architecture/` Pest arch tests enforcing CLAUDE.md §3a (see §9)
- [x] `tests/Fakes/` directory + fake for every external-service interface
- [!] One vertical slice as a reference implementation (FormRequest → DTO → Action → Repository → Inertia Resource) for later tasks to copy — deferred to Phase 1 by human decision: every real concept to slice through belongs to a later phase, and a throwaway non-domain example couldn't demonstrate tenancy scoping (CLAUDE.md §2). Moved to Phase 1 below, once the first tenant-scoped model exists.
- [x] Base Inertia layouts: `GuestLayout`, `AppLayout`, `AdminLayout` (Bootstrap, no theming yet)
- [x] Error pages (403/404/419/500) as Inertia pages
- [!] Ploi deployment script + zero-downtime deploy, staging environment on Hetzner — deploy script and docs done (`deploy/ploi-deploy.sh`, `docs/deployment.md`); actually provisioning the Hetzner server, Ploi site, and staging environment needs your Ploi/Hetzner account access — see `docs/deployment.md` for the exact steps
- [!] Queue worker (Redis) + Horizon, scheduler cron via Ploi — Horizon installed and configured, `QUEUE_CONNECTION=redis`; the actual queue worker daemon and scheduler cron registration are Ploi dashboard actions — see `docs/deployment.md`
- [!] Sentry (or equivalent) with PII redaction configured — SDK installed and wired (`send_default_pii => false`, `SentryPiiRedactor` before_send hook, exception handler integration); a real `SENTRY_LARAVEL_DSN` needs your Sentry account

---

## Phase 1 — Tenancy, auth, roles

### Tenancy core

- [x] `resellers` table + model (name, slug, status, settings, soft deletes)
- [x] `App\Tenancy\TenantContext` scoped singleton
- [x] `App\Scopes\TenantScope` global scope
- [x] `App\Concerns\TenantScoped` trait (scope + `creating` stamp) — Larastan flags an unused trait with no consumer, so `resellerklanten` (table + model only, see below) was pulled forward as its first real consumer, with an isolation test in `tests/Tenancy/`
- [x] `ResolveTenant` middleware: custom domain → cookie → fallback — both steps now fully wired (this task added the custom domain step)
- [x] `/login/{slug}` route that resolves reseller and sets the tenant cookie
- [x] Custom domain table + verification flow (DNS CNAME check)
- [!] Automated LetsEncrypt issuance for custom domains (Ploi API) — interface, Action, event/listener wiring, and fake all done and tested; `HttpPloiClient`'s endpoint/payload are written to best understanding of Ploi's API but marked `VERIFY:` since untested against a live account — needs your Ploi credentials to confirm
- [x] Unbranded fallback experience for bare `leerportaal.nl`
- [x] `tests/Tenancy/` helper: `actingAsReseller()`, `assertTenantIsolated()`
- [x] Artisan command `tenancy:audit` — fails if any tenant table lacks `reseller_id` or the trait

### Users & auth

- [x] `users` table: one table, one guard, `reseller_id` nullable (null = platform staff)
- [x] `resellerklanten` table + model, `reseller_id` scoped — done early (Tenancy core section above)
- [x] `users.resellerklant_id` nullable FK
- [x] Reference vertical slice (FormRequest → DTO → Action → Repository → Inertia Resource) built around `resellerklanten` — index + create, `/klanten`, now gated behind real `auth` middleware
- [x] Login, logout, password reset, email verification (Fortify or hand-rolled) — hand-rolled by human decision, using the same Action/FormRequest/Controller pattern as everywhere else; login is tenant-aware (branded domain restricts to that reseller + platform staff, unbranded restricts to platform staff only)
- [x] 2FA (TOTP + recovery codes). Mandatory for platform roles, optional for others. `reseller_id === null` used as a proxy for "platform role" until task 34 (spatie/laravel-permission) exists
- [x] Rate limiting on all auth endpoints
- [x] Session security: regeneration, secure cookies, sensible lifetime. Regeneration was already in place from the login/2FA/logout work; added a production-defaulting `secure` cookie flag (defense in depth even if the host env var is never set) and kept the 120-minute default lifetime
- [x] Invite flow: reseller/klant invites user by email, signed branded link. `user_invites` table (tenant-scoped) + `App\Enums\Role` (the full task-34 role list, stored but not yet enforced -- task 34 should seed these exact cases rather than reinventing them). Accept link carries the reseller's slug (`/invite/{reseller}/accept/{invite}/{hash}`) so the guest's request can re-resolve TenantContext before any tenant-scoped read happens, same bootstrapping idea as `/login/{slug}` -- avoids ever needing `withoutTenantScope()` for a non-admin context

### Roles & permissions

- [x] Install `spatie/laravel-permission`, teams mode, `team_foreign_key = reseller_id`. `TenantContext::set()` now also calls `PermissionRegistrar::setPermissionsTeamId()`, so every call site that resolves tenant (middleware and the invite-accept flow) keeps spatie's team scoping in sync automatically. Open question before role seeding: `model_has_roles`/`model_has_permissions.reseller_id` is NOT NULL (and part of the composite PK) once teams mode is on, so platform staff (`reseller_id` null) cannot be assigned any spatie role as currently configured -- needs a decision before task 35
- [x] Seed roles: `super-admin`, `platform-admin`, `platform-author`, `support`, `reseller-owner`, `reseller-admin`, `reseller-author`, `reseller-reporter`, `klant-admin`, `klant-manager`, `cursist`. Resolved the open question from the previous line by human decision: platform roles (`super-admin`, `platform-admin`, `platform-author`, `support`) are stored on `users.platform_role` directly, not as spatie roles -- avoids fighting teams mode's NOT NULL team column. The 7 reseller/klant roles are real spatie roles, auto-seeded per reseller via a `ResellerCreated` event fired from `Reseller`'s `created` model event (`App\Actions\Permissions\SeedRolesForReseller`); `permissions:sync-roles` artisan command backfills any reseller that predates this wiring
- [x] Seed granular permissions (courses._, users._, reports._, billing._, impersonate). `App\Enums\Permission` is the catalog, seeded as global spatie Permission rows via a data migration (not a seeder -- must exist via `php artisan migrate` alone, matching how deploys actually run). Not assigned to any role yet: courses/reports/billing have no model or Policy to consume them until later phases, so there's nothing to assign them to yet
- [x] Base `Policy` for every model. `Gate::before` for super-admin only. Added the 3 missing policies (`CustomDomain`, `User`, `Reseller`) following the same "placeholder until roles exist" pattern as the existing ones. `Gate::before` lives in `App\Policies\SuperAdminBypass` (an invokable class), not inline in `AppServiceProvider`, so the `User` type-hint it needs stays inside the arch whitelist without having to widen it to all of `App\Providers`
- [x] Feature tests: each role can/cannot reach each area. Tests today's actual (coarse) model, by human decision: the 7 team roles are seeded but nothing checks them yet (every policy still checks reseller_id !== null / platform_role), so they're genuinely interchangeable right now -- confirmed via `/klanten` and `/invites` for every team role, no-role, every platform_role, and the super-admin bypass. Role-specific access is a later-phase concern once real features have policies that check `hasRole()`/permissions

### Audit & impersonation

- [x] Install `spatie/laravel-activitylog`
- [x] `HasAuditLog` trait, applied to all user/content/billing models. Applied to every current model (`User`, `Reseller`, `ResellerKlant`, `UserInvite`, `CustomDomain`) -- no content/billing models exist yet, future ones should pick up the trait too. Uses `logUnguarded()` since every model uses `$guarded = []` rather than `$fillable`. Secrets (password, remember_token, two_factor_secret, two_factor_recovery_codes) are excluded globally via `config('activitylog.default_except_attributes')`, not per model, so nothing new can accidentally leak them by reusing a column name
- [x] Impersonation: start/stop, reason required, timestamped, session limit, UI banner. Hierarchy by human decision: super-admin -> any reseller-side user; reseller staff -> their own klanten/cursisten (not fellow staff); klant-admin/klant-manager -> cursisten in their own klant only (the first real consumer of the spatie team roles seeded back in task 34/35). 15-minute hard session limit, enforced by `EnforceImpersonationSessionLimit` middleware in the `web` group. Platform staff can never be impersonated
- [x] Block password change, billing, and permission changes while impersonating. `BlockDuringImpersonation` middleware (alias `block-during-impersonation`) built and tested, but not attached to any route yet -- none of those three areas has a route in this phase (self-service password change, billing, and permission-management UI are all later phases). Attach it to their routes once they exist
- [x] Soft deletes on all user-facing models + restore actions. Soft deletes were already in place on every model (`User`, `Reseller`, `ResellerKlant`, `UserInvite`, `CustomDomain`) from when each was built. Added delete + restore Actions/routes/UI for the two models that actually have a working delete flow today (`ResellerKlant`, `UserInvite`) -- `Reseller`, `CustomDomain`, and `User` have no delete UI at all yet (Phase 6 admin panel, no management UI, and no user-management UI respectively), so there's nothing to restore from; add their restore actions alongside whenever their delete UI gets built

---

## Phase 2 — Whitelabel Level 1

- [x] `reseller_themes` table: primary/secondary/accent colors, font family, logo, favicon, login background. Tenant-scoped like every other reseller-owned table (isolation test included); `primary_color` defaults to Bootstrap's own primary blue so an uncustomized reseller still renders sensibly. Upload/validation for logo/favicon/login background comes with the theme editor UI task -- this task is schema + model only
- [x] Runtime CSS custom property injection into layouts (no per-tenant build). `App\Services\Theming\ThemeCssGenerator` renders `--reseller-*` custom properties from a `ResellerTheme`, re-validating every value itself (never trusts the database contents, since it's interpolating into a raw `<style>` block) -- added `App\Services` to the "who may touch Models" arch whitelist for this, since CLAUDE.md's own Service examples (GradingService, etc.) need the same. Wired via a `View::composer('app', ...)` in `AppServiceProvider`, computed fresh per request from `TenantContext`
- [x] Bootstrap `_variables.scss` wired to CSS custom properties. New `_theme-overrides.scss`, loaded after Bootstrap, maps `--reseller-*` onto the CSS variables Bootstrap 5.3 already exposes at `:root` (`--bs-primary`, `--bs-primary-rgb` via CSS relative-color syntax, `--bs-link-color`, `--bs-font-sans-serif`, etc.) so live components re-theme per request with no rebuild. Known, documented limitation: Bootstrap's hover/active shade variants (e.g. `--bs-btn-hover-bg`) are Sass-computed at build time, not live `var()` refs, so they can't follow a runtime color change -- inherent to "no per-tenant build step" (CLAUDE.md §1), not a bug
- [x] Theme editor UI with live preview. `/settings/theme` (reseller-side only, same placeholder policy pattern as everywhere else). Live preview is genuinely live: color/font inputs write straight to `document.documentElement`'s `--reseller-*` custom properties on change, so Bootstrap's own components re-theme instantly pre-save, using the exact mechanism `ThemeCssGenerator` uses server-side -- no separate preview pane needed. `ResellerThemeRepository::findForCurrentResellerOrDefault()` gives the editor a fresh unsaved instance with the schema's own defaults when the reseller has no theme row yet, so the form always has something to pre-fill
- [x] Contrast validation — warn when a chosen color pair fails WCAG AA. Client-side, non-blocking (a warning, not a rejection, per the task's own wording): `resources/js/lib/contrast.ts` implements the WCAG relative-luminance formula directly from spec, checked against primary/secondary vs white (the pairing that actually matters here, since Bootstrap renders white text on those backgrounds by default). Verified against the two values the formula is mathematically guaranteed to produce (black/white = 21:1, identical colors = 1:1) -- no JS test runner exists in this project yet, so this was checked by hand rather than adding a whole new test framework for one utility
- [x] Logo/favicon upload with dimension + type validation, private disk. Human decision on the private-disk-vs-public-visibility tension: stored on the private `local` disk (CLAUDE.md §7) but served through a new public, unauthenticated route (`App\Http\Controllers\ResellerBrandingController`, `/branding/{reseller}/logo|favicon|login-background`) that resolves the reseller by slug and streams the file back -- so the disk itself never gets a directly guessable public URL, but the login page can still show it. Type validated by content (`image`/`mimes` rules sniff actual file signatures) rather than extension, per §7. SVG deliberately not accepted for logo/login-background -- its dimensions aren't reliably introspectable via `getimagesize()`, which would make the `dimensions` rule either flaky or a no-op. `[!]` Two feature tests (full successful-upload path, needs a real generated image) can't run on this dev machine -- the GD PHP extension isn't installed locally (confirmed: the DLL exists but is commented out in `php.ini`); added `gd` to `.github/workflows/ci.yml`'s extensions list so they run in CI, which is where they'll actually be verified. Didn't edit the machine's `php.ini` myself -- that's a system-level change outside this repo, flagging it for you instead: uncomment `extension=gd` in `C:\php85\php.ini` if you want these to run locally too
- [x] Optional custom CSS field, sanitized, with a hard character limit. New `custom_css` nullable text column on `reseller_themes`. Chose rejection over silent sanitization: `App\Rules\RejectsUnsafeMarkup` (new `App\Rules` namespace; originally named `SafeCustomCss`, renamed when the email-templates task below reused it for Markdown body validation -- the denylist isn't CSS-specific) rejects, via a `$fail` message, values containing `</style` (breakout -- the critical one), `<script`, `@import`, `expression(`, `behavior:`, `javascript:`, `vbscript:`, or `data:text/html`, rather than quietly stripping them -- silently mangling admin-submitted CSS is worse UX and less safe than a clear rejection with a reason. 10,000-character hard limit on the field itself. `ThemeCssGenerator` appends the value after the `:root` block, re-running the same denylist itself before rendering (exposed as `RejectsUnsafeMarkup::firstViolation()`, reused by both the write-side rule and this read-side check) so a row that somehow predates the rule still can't reach the page -- consistent with the generator's existing "never trust the database" stance for colors/fonts. Live preview in the theme editor injects the typed CSS into an actual `<style>` tag in the current document (not a security boundary, just a mirror of the color-field preview pattern -- the server-side rule is what actually gates what gets persisted and served to other visitors)
- [x] Per-reseller email branding: logo, colors, sender name, reply-to. Logo/colors are already `reseller_themes` fields from earlier in this phase -- no duplication needed, email templates can read the same row. Added `sender_name` and `reply_to_email` (nullable) columns to that table rather than a new one, since it's the same "reseller brand identity" concept. Human-judgment call, documented: the underlying "from" *address* always stays the platform's own verified address regardless of `sender_name` -- this app has no per-reseller domain verification (SPF/DKIM) yet, so sending "from" an unverified reseller address would hurt deliverability or get flagged as spoofed. Only the display name recipients see, and where replies go, are reseller-configurable, which matches the task's literal wording ("sender name", not "sender address"). New `App\Mail\Concerns\HasResellerBranding` trait (reusable by future Mailables) builds the `Envelope`; wired into the existing `UserInvited` mailable. Theme is fetched eagerly in the Mailable's constructor via a new `ResellerThemeRepository::findForReseller(int $resellerId)` method (bypasses ambient `TenantContext` on purpose, via `withoutTenantScope()` -- explicitly parameterized by the caller's own reseller ID, so it's not weakening isolation, just working around the fact that queued mail runs in a worker process with no request and therefore no ambient tenant context at all)
- [x] Per-reseller email templates (overridable per notification type). Human decision: subject + full Markdown body override (not subject-only or plain-text-paragraph -- the other two options considered). Split into sub-tasks since this is real scope (a new registry, table, safe-rendering pipeline, and editor UI), not one commit:
  - [x] `App\Enums\MailTemplateType` registry: one case per overridable notification (starts with `UserInvited`), each carrying `label()`, `placeholders()` (token => description, the fixed whitelist substitution is ever allowed against), and `defaultSubject()`. `defaultSubject()` is illustrative only, shown as a hint in the editor UI -- it deliberately does NOT replace the existing default-sending path (`UserInvited` still uses its own Blade view + `trans()` call when there's no override), to avoid regressing that already-working, already-tested behavior while building this out
  - [x] `reseller_mail_templates` table + model (tenant-scoped, isolation test) + repository (`findForCurrentReseller()` for the editor, `findForResellerAndType()` bypassing ambient `TenantContext` for Mailables, same reasoning as `ResellerThemeRepository::findForReseller`). `subject`/`body_markdown`/`type` are NOT NULL -- a row only exists when a reseller has actually overridden that type, `unique(reseller_id, type)` enforces one override per type
  - [x] Safe rendering pipeline: `App\Support\Mail\PlaceholderRenderer` (plain `preg_replace_callback` against the type's fixed whitelist -- a callback, not a replacement string, so a placeholder value containing `$1`-style text can't be misread as a backreference) and `App\Services\Mail\SafeMarkdownRenderer` (its own `CommonMarkConverter` with `html_input => 'escape'` + `allow_unsafe_links => false`, not a reuse of `Illuminate\Mail\Markdown` -- that class is configured for developer-authored Blade views, not untrusted input, and reusing it would mean re-auditing how it handles raw HTML passthrough). `App\Services\Mail\MailTemplateRenderer` combines both plus a small standalone Blade wrapper (`resources/views/emails/templates/overridden.blade.php`) for override bodies -- deliberately NOT reusing Laravel's `mail::message` component pipeline, to keep the untrusted-content code path small and fully auditable rather than threading it through framework internals built for trusted input
  - [x] Wire `UserInvited` to use the reseller's override when one exists (subject template + Markdown body, both placeholder-substituted), falling back to the existing default Blade view + `trans()` subject otherwise -- unchanged behavior for resellers with no override
  - [x] Editor UI: `/settings/email-templates` lists every type with its overridden/default status; `/settings/email-templates/{type}` (backed-enum route binding) has the subject + Markdown body form, a placeholder-token reference list, a server-rendered HTML preview (using the type's sample placeholder values -- `MailTemplateRenderer::renderPreview()`, not a live client-side round trip, to avoid a second rendering pathway), and a reset-to-default button that submits both fields empty
  - [x] Tests: repository, action, policy, renderer (unsafe Markdown/HTML input is escaped, not executed), full FormRequest → DTO → Action → Repository chain including the reset-to-default and both-fields-required-together cases, Mailable envelope/content with and without an override

Human decision on scope: the "sender name, reply-to" branding task above and this one both needed the same denylist rule, which was originally named `SafeCustomCss` -- renamed to `App\Rules\RejectsUnsafeMarkup` when it stopped being CSS-specific, with every call site (theme request/generator, this request) updated in the same commit as the rename.
- [x] Configurable footer content, support email, terms/privacy URLs. Four new nullable columns on `reseller_themes` (`footer_text`, `support_email`, `terms_url`, `privacy_url`) -- same "reseller brand identity" table as the rest of this phase, not a new one. `footer_text` reuses `RejectsUnsafeMarkup` (it's rendered on every page, same escape-vector concern as custom CSS/mail bodies). Shared globally via a `footer` key in `HandleInertiaRequests::share()` (new pattern for this codebase -- existing shared props like `appName`/`flash`/`impersonation` are all in that same file, so this follows it rather than inventing a second mechanism), read through `ResellerThemeRepository::findForCurrentReseller()` fresh per request. New `resources/js/Components/AppFooter.vue`, mounted in both `AppLayout.vue` and `GuestLayout.vue` so it appears on authenticated app pages and the public login page alike; renders nothing when a reseller has set none of the four fields
- [x] Feature test: reseller A's theme never leaks into reseller B's render. Added to `tests/Feature/ThemeInjectionTest.php`: renders as reseller B (with A and B both having distinct themes, including a `custom_css` value unique to A), asserts B's own primary color appears and A's color/custom CSS do not -- the existing tests in that file only covered single-reseller and unbranded-fallback cases, none exercised two resellers in the same test

---

## Phase 3 — Course content

- [x] `course_categories` (nested, platform-level + reseller-level). Schema + model + repository only, matching this phase's earlier precedent (e.g. `reseller_themes`) of building the data layer before any admin UI, which belongs to Phase 6. `reseller_id` nullable (null = platform category, visible to every reseller; set = owned by one reseller only) and self-referencing `parent_id` for nesting. Architecture decision: `CourseCategory` deliberately does NOT use `TenantScoped` -- the trait's global scope is strictly single-owner and fails closed (`WHERE reseller_id = ?`), which can't express "platform rows visible to everyone." Instead, `App\Contracts\Repositories\CourseCategoryRepository::visibleToCurrentReseller()` composes the visibility explicitly (`WHERE reseller_id IS NULL OR reseller_id = ?`) in `EloquentCourseCategoryRepository`. Added to `tenancy:audit`'s `EXEMPT` list alongside `User`, which already exists for the identical reason (one table mixing platform and reseller-owned rows). Custom visibility test in `tests/Tenancy/` (not the standard `assertTenantIsolated()` helper, since that assumes strict single-ownership) covers: platform rows visible to every reseller, a reseller's own rows invisible to other resellers, and platform-only visibility when no tenant is resolved. This same pattern will apply to the next task (`courses`, which has the identical "owner type platform/reseller, `reseller_id` nullable" shape)
- [x] `courses` table: title, slug, description, status, owner type (platform/reseller), `reseller_id` nullable, `available_locales`, `variant_year`, `repeats_from_course_id`, price fields. Same mixed-ownership shape and pattern as `course_categories` (no `TenantScope`, visibility composed in `EloquentCourseRepository`, added to `tenancy:audit`'s `EXEMPT` list) -- no separate `owner_type` column, since `reseller_id` nullable already encodes it and a redundant column would risk drifting out of sync with no real benefit; `course_categories` established the same convention immediately prior. `status` is a new `CourseStatus` enum (`draft`/`in_review`/`published`) -- only the states, not the transition/versioning workflow, which is its own later task. `available_locales` is already a plain `json`/array cast; `title`/`description` stay plain strings for now, becoming translatable JSON in the dedicated later task, per the phase's own task ordering. First money-bearing table in the codebase, so built the `Money` value object CLAUDE.md §4 requires (deliberately minimal -- cents + equality/zero check, no arithmetic yet, since nothing needs it until the Phase 6 billing calculations) and an `App\Casts\MoneyCast` bridging the three `_cents` integer columns (`reseller_set_price_cents`, `platform_price_cents`, `price_floor_override_cents` -- the last one is the platform admin's manual floor override from CLAUDE.md §11, applies regardless of owner) to it. No `course_category_id` FK yet -- not in the task's field list, so not added; will follow when an actual consumer needs it rather than guessing at the relationship shape now
- [x] `modules` → `lessons` → `blocks` hierarchy, all ordered. Schema + models only (no repositories -- unlike Course/CourseCategory there's no hybrid-visibility logic here needing one yet; a future consumer would add one when it has real query logic to justify it, per CLAUDE.md §3a). None of the three tables carries `reseller_id`: ownership lives once on `Course`, and modules/lessons/blocks inherit visibility transitively through `course_id`/`module_id`/`lesson_id` -- duplicating the column down the hierarchy would just risk it drifting out of sync with the course's own value, and since there's no `reseller_id` column at all, `tenancy:audit` doesn't need an exemption entry for any of them (same as any platform-owned model). Each level has its own `order` column. Cascade-deletes down the chain on hard delete (`course_id`/`module_id`/`lesson_id` all `cascadeOnDelete()`), independent soft deletes otherwise. `blocks.type` is a new `BlockType` enum with the 7 cases from the next task's list (`rich_text`, `image`, `video_embed`, `file_download`, `embed`, `divider`, `callout`) and a generic `content` JSON column -- same split as `CourseStatus`: the states/type names exist now, the actual per-type editor/player components and content-shape validation are the next task's job
- [x] Block types: rich text, image, video embed, file/download, embed, divider, callout. Listed on one TODO.md line (unlike Phase 4's question types, itemized on 15 separate lines each meant to be its own commit) -- built all 7 together in one commit. Mirrors CLAUDE.md §5's `QuestionType` shape exactly: `App\Blocks\Contracts\BlockType` interface (`label()`, `contentRules()`, `editorComponent()`, `playerComponent()`), `App\Blocks\BlockTypeRegistry` (enum case -> definition class), one class per type in `App\Blocks\Types`. Renamed the schema task's `App\Enums\BlockType` to `BlockTypeEnum` first -- it would otherwise collide with this task's `BlockType` interface name, same split CLAUDE.md §5 already uses (`QuestionType` interface vs `QuestionTypeEnum` enum). `contentRules()` reuses `RejectsUnsafeMarkup` for rich text (a fourth consumer now) since it's rendered the same way custom CSS/mail bodies are; image/file/video/embed URLs are plain validated strings, not uploads -- the media library task later in this phase is what adds real uploads, and this content shape doesn't need to change when it does. 14 new Vue components (`resources/js/Components/Blocks/{Editor,Player}/*.vue`), each using Vue 3.4's named `defineModel()` per content field rather than one object-shaped model -- mutating a nested property of a single object model doesn't reliably trigger `defineModel`'s emit, only reassigning the whole ref does. No consuming authoring/player page exists yet (none is itemized anywhere in this file -- confirmed by reading the full remaining TODO.md before starting this task), so these components aren't imported/bundled by anything yet; `vue-tsc` still type-checks them standalone. Registry test asserts every enum case resolves to the right class and that every type has a non-empty label and both component names; content-rules test exercises valid/invalid payloads per type via Laravel's `Validator`
- [x] Install `spatie/laravel-translatable`; translatable fields as JSON. Applied to the two user-facing text fields that exist so far: `Course` (`title`, `description`) and `CourseCategory` (`name`) -- both now use `HasTranslations` with `public array $translatable`. `courses.title` and `course_categories.name` were widened from `string` (255) to `text` (a `{"nl": "...", "en": "..."}` map can exceed 255 chars across locales; `courses.description` was already `text`); done via drop-and-recreate in the migration rather than `Blueprint::change()`, since that needs `doctrine/dbal`, not installed, and both tables are brand new with no data yet, so it's safe. Deliberately did NOT touch `blocks.content`: each block type has its own JSON shape (rich text's `html`, callout's `text`+`variant`, etc.), and translating a whole nested structure per locale is a materially different design problem than translating a single string column -- spatie/laravel-translatable is built for the latter. Left as a documented gap for the next two tasks (per-field locale dropdown, translation completeness) to actually solve, rather than bolting on something that wouldn't really work. Plain string assignment (`$course->title = 'x'`) continues to work unchanged -- `HasTranslations` intercepts it and stores it under the current app locale, so no factory changes were needed
- [x] Per-field language dropdown in the editor (per `available_locales`). Same situation as the block-types task: no course/lesson editor page is itemized anywhere in this file yet (that's an admin-authoring surface, presumably Phase 6, though not explicitly named there either), so this is a reusable standalone Vue component, not a page. New `resources/js/Components/LocaleField.vue` takes a `TranslatableValue` (`Record<string, string>`, the exact shape `spatie/laravel-translatable` returns/stores) as its `v-model`, plus an `availableLocales: string[]` prop the caller supplies (e.g. a course's own `available_locales`) -- shows a locale dropdown only when there's more than one locale to switch between, otherwise just the plain field. Uses a `computed` get/set pair keyed by the active locale rather than binding straight to a nested property of the model object, for the same reason the block Editor components do: mutating a nested property doesn't reliably trigger `defineModel`'s emit, only reassigning the whole ref does
- [x] Translation completeness indicator; block publish in a locale until complete. `App\Services\Translation\TranslationCompletenessService` answers "is this model's translatable content complete for this locale" (every `getTranslatableAttributes()` entry present and non-blank) -- it does NOT gate anything itself. New `App\Contracts\HasTranslatableFields` interface exists because `Spatie\Translatable\HasTranslations` is a trait, not an interface, so it can't be type-hinted directly; `Course` and `CourseCategory` now implement it alongside using the trait (the trait already satisfies both method signatures). Actually enforcing "don't publish in a locale until complete" is the publish Action's job, wired in when the draft/review/published workflow (the very next task) builds that Action -- there's nothing to enforce it in yet. `resources/js/Components/TranslationCompletenessIndicator.vue` is, like the block-types and locale-dropdown tasks before it, a standalone reusable component (badge per locale, complete/incomplete) with no consuming page yet, for the same reason: none is itemized in this file
- [x] Draft → review → published workflow with versioning. Three Actions, one per transition (`SubmitCourseForReview`: Draft → InReview, `RevertCourseToDraft`: InReview or Published → Draft -- one Action covers both since they collapse to the same transition, `PublishCourse`: InReview → Published), each taking the `Course` model directly rather than a DTO -- there's no FormRequest/route yet (no admin UI task exists for this either, same recurring situation as the last several tasks), and a state-transition Action operating on an existing entity doesn't need one. Invalid transitions throw a new `InvalidCourseStatusTransitionException`. Human-judgment scope call on "versioning": deliberately just an incrementing `version` integer + `published_at` timestamp, NOT full content snapshots -- nothing consumes historical versions yet (no enrollment/assignment exists to pin a cursist to a specific version), so snapshotting the whole content tree now would be building for a hypothetical need; Phase 5 can add that when assignment actually requires it. `PublishCourse` enforces the "block publish in a locale until complete" half of this task by checking `TranslationCompletenessService::isComplete()` for every one of the course's `available_locales`, throwing a new `IncompleteCourseTranslationsException` listing exactly which locales are incomplete if any fail -- this is the first real consumer of that service, wired exactly where the previous task's note said it would be
- [x] Course duplication (within a reseller, and platform-internal). Resolved "within a reseller, and platform-internal" using CLAUDE.md §1's own settled decision: catalog courses are read-only for resellers with no forking/copy-on-write, so duplication must never cross the platform/reseller ownership boundary -- achieved simply by never overriding `reseller_id` on the copy (`replicate()` carries it over unchanged). `App\Actions\Courses\DuplicateCourse` takes a `Course` directly (same no-DTO reasoning as the workflow Actions), copies the full module → lesson → block tree, resets the copy to `Draft`/`version` 0/`published_at` null, deliberately does NOT copy `variant_year`/`repeats_from_course_id` (a repeat is a different, billing-relevant concept per §11), appends "(Copy)" to the title in every locale it's translated in, and de-duplicates the slug with an incrementing suffix -- checked via `withTrashed()` since the `slug` unique index isn't scoped to exclude soft-deleted rows, so a trashed course could otherwise still collide. Uses `$model->relation()->associate($model)` rather than assigning `->id` to the `_id` foreign key property directly -- Larastan flagged the latter as a genuine type mismatch (`id` is generically `int`, the FK columns are Larastan's narrower unsigned `int<0, max>`), and `associate()` is the more idiomatic Eloquent fix, not a workaround
- [x] Media library per reseller, S3-compatible storage, private disk + signed URLs. Same mixed-ownership shape as `course_categories`/`courses` (nullable `reseller_id`, no `TenantScope`, visibility composed in `EloquentMediaRepository`, added to `tenancy:audit`'s `EXEMPT` list). Installed `league/flysystem-aws-s3-v3` -- the `s3` disk config already existed from the Laravel skeleton and was already generically S3-compatible (`AWS_ENDPOINT`/`AWS_USE_PATH_STYLE_ENDPOINT` work against any S3-compatible provider, e.g. Hetzner Object Storage, not just AWS), just needed the adapter package and `'visibility' => 'private'` added explicitly. `App\Actions\Media\UploadMedia` stores under a `platform/` or `reseller-{id}/` prefix depending on ownership. `App\Services\Media\MediaUrlSigner` generates temporary signed URLs -- `Illuminate\Contracts\Filesystem\Filesystem` doesn't declare `temporaryUrl()` (only the concrete `FilesystemAdapter` every real driver returns does), so this uses a genuine `instanceof` runtime guard to narrow the type safely, not a cast or docblock override
- [x] Storage usage metering per reseller (5 GB included). CLAUDE.md §3a explicitly lists "storage metering" as a mandatory interface, so this follows the same pattern as `PaymentGateway`: `App\Contracts\Storage\StorageMetering` (`usageBytes()`, `includedBytes()`, `isOverLimit()`), real `App\Services\Storage\EloquentStorageMeteringService` bound in `AppServiceProvider`, `Tests\Fakes\FakeStorageMetering` bound by default in `tests/TestCase.php` (so no test can accidentally sum real media rows to simulate a usage scenario) with its own dedicated test matching the existing `FakePaymentGatewayTest` precedent. `usageBytes()` never counts platform media -- platform storage isn't billed to any reseller. New `MediaRepository::totalBytesForReseller()` does the actual `SUM(size_bytes)`, explicitly parameterized by reseller ID rather than ambient-tenant-scoped so it works for any reseller (e.g. future platform admin reporting), not just the current one
- [x] Video: external hosting (Vimeo/Mux/YouTube unlisted). Do not self-host video. The block-types task already made self-hosting structurally impossible (`video_embed`'s `url` field is a plain string, never a file reference), but didn't yet restrict *which* external hosts it could point at. New `App\Enums\VideoProvider` (`vimeo`/`mux`/`youtube`) with `fromUrl()` doing host-suffix matching (`str_ends_with($host, 'vimeo.com')` etc., handling `youtu.be` as a `YouTube` alias), and `App\Rules\AllowedVideoProvider` (a `ValidationRule`) rejecting any `url` that doesn't resolve to one of them. `VideoEmbedBlock::contentRules()`'s `url` rule gained `new AllowedVideoProvider`; its existing free-form `provider` string field is now constrained to the same enum via `Rule::enum()` rather than left arbitrary, since an unvalidated `provider` string could otherwise silently disagree with the actual (validated) URL
- [x] Course prerequisites and ordering rules. Two independent concerns bundled in one commit, same as the block-types task. **Prerequisites**: new `course_prerequisites` pivot table (`course_id`, `prerequisite_course_id`, unique pair, both FK cascade-on-delete), `Course::prerequisites()`/`requiredFor()` `BelongsToMany` self-relations. `App\Actions\Courses\AddCoursePrerequisite` takes both `Course` models directly (same no-DTO reasoning as the workflow/duplication Actions) and does BFS cycle detection over the prerequisite graph before linking -- a self-reference or any circular chain (direct or transitive) throws a new `CircularCoursePrerequisiteException`, since an uncheckable cycle would make "has this cursist satisfied the prerequisites" unanswerable later. No ownership restriction between reseller/platform courses -- a reseller course requiring a catalog course as a prerequisite is a reasonable content decision, not a tenancy violation. `RemoveCoursePrerequisite` is a plain detach, no cycle check needed. **Ordering**: modules/lessons/blocks all share the identical "ordered `HasMany` of a parent" shape, so one `App\Services\Ordering\SiblingOrderingService::reorder()` (validates the given ID list is an exact match for the existing siblings, throws `InvalidOrderingException` otherwise, then writes each row's `order` to its index) is reused by three thin Actions (`ReorderModules`, `ReorderLessons`, `ReorderBlocks`), each just supplying the correctly-scoped `HasMany` relation -- which is also what stops an ID from a different parent silently sneaking into another parent's ordering, since the mismatch check runs against that scoped relation, not a global table scan. Takes a new `App\DataTransferObjects\Courses\ReorderItemsData` (`list<int> $orderedIds`) since, unlike the state-transition Actions, this Action needs real external input beyond the model itself
- [x] Estimated duration, learning objectives, tags. `courses` gains `estimated_duration_minutes` (nullable int) and `learning_objectives` (nullable `json`, plain `array` cast, `list<string>`). Deliberately did NOT make `learning_objectives` translatable/add it to `Course::$translatable`, even though it's technically user-facing text -- `TranslationCompletenessService` checks *every* translatable field to gate publishing, so doing that would have silently made publishing stricter for every existing course (and broken the previous task's already-settled completeness tests) for a task that only asked for the field to exist. If per-locale objectives become a real need, that's a small, deliberate follow-up. **Tags**: new `Tag` model + `tags`/`course_tag` tables, platform-wide shared vocabulary -- no `reseller_id` at all (unlike `CourseCategory`/`Course`/`Media`'s mixed-ownership shape), since a common tag taxonomy searchable across every reseller's catalog is more useful than one duplicated per reseller; no `tenancy:audit` exemption needed for the same reason modules/lessons/blocks don't need one (no `reseller_id` column present at all). `Course::tags()` / `Tag::courses()` plain `BelongsToMany`
- [x] Catalog courses are **read-only** for resellers — enforced by policy + tested. New `App\Policies\CoursePolicy`, same "placeholder until roles exist (Phase 1 task 34)" style as the other current policies -- `view`/`create` are open to any authenticated user (visibility itself is `EloquentCourseRepository`'s job, not the policy's), and the one real rule is CLAUDE.md §1's settled "no forking, no copy-on-write": `update`/`delete`/`restore` on a catalog course (`reseller_id` null) require the acting user to also be platform staff (`reseller_id` null); a reseller-owned course requires the user's `reseller_id` to match the course's. Super admin still reaches every action via the existing `Gate::before`/`SuperAdminBypass`, already covered by its own generic test, so not re-tested per-policy here

---

## Phase 4 — Questions & assessment

- [x] `questions` table per CLAUDE.md §5. Bundled into the same commit as the `quizzes` table task below it, out of the order they're listed in -- CLAUDE.md §5's own schema has `questions.quiz_id`, a hard FK dependency on a table that otherwise wouldn't exist until the third task down; splitting them into separate commits would mean the first commit either has no real FK or references a table that doesn't exist yet, neither of which is better than just building the two coupled schemas together (same reasoning `course_categories`/`courses` etc. get compared/updated together when they share a real dependency). Columns: `quiz_id`, `type` (plain `string` for now -- `QuestionTypeEnum` is the next task's job, same split `BlockTypeEnum` got from the block-types task), `prompt` (translatable, same JSON-per-locale-as-text-column shape as `courses.title`), `points`, `order`, `settings` (json), `payload` (json). No `reseller_id`: ownership is inherited transitively through `quiz -> module|lesson -> course`, same as modules/lessons/blocks
- [x] `QuestionType` interface, `QuestionTypeRegistry`, `QuestionTypeEnum`. `App\Enums\QuestionTypeEnum` gets all 14 cases from the type list below at once (cheap, and every later "Implement type: X" task needs its case to already exist). `App\Questions\Contracts\QuestionType` was a Phase 0 placeholder (loose `array` payload/grade shape, no `Question`/`GradeResult`/`QuestionTypeEnum` yet, and its own docblock said as much) -- firmed up now to CLAUDE.md §5's literal signature, safe since nothing implemented it yet. New `App\DataTransferObjects\Questions\GradeResult` (named exactly as the interface's `grade()` return type specifies, not `GradeResultData`) -- nullable `pointsAwarded`/`isCorrect()` so a manual-grading type (essay, file upload) can report "pending" rather than a fake score. `App\Questions\QuestionTypeRegistry` mirrors `BlockTypeRegistry`'s "enum case -> class-string" map exactly, except it deliberately starts **empty**: unlike blocks (all 7 types landed in one commit), every question type below is its own separate task/commit, so an empty/partial map is the correct intermediate state for every commit until the last type lands, not a bug -- `resolve()` throws a clear `OutOfBoundsException` for anything not yet registered rather than an undefined-array-key error. `QuestionType::grade()` taking a `Question` model directly (per CLAUDE.md §5's own signature) needed `App\Questions` added to `tests/Architecture/LayeringTest.php`'s "who may touch Models" whitelist, same justification already on record there for `App\Services`
- [x] `quizzes` table: type (practice/exam), settings, `lesson_id` or `module_id`. See the note on the `questions` task above for why these two are one commit. `module_id`/`lesson_id` are both nullable FKs; exactly one must be set (a quiz is either an in-lesson knowledge check or an end-of-module exam) -- enforced via a `static::saving` guard in `App\Models\Quiz` throwing a new `InvalidQuizParentException`, not a DB `CHECK` constraint, since constraint syntax/support isn't uniformly portable across the SQLite-in-memory (tests) / MySQL (prod) split CLAUDE.md §8 requires. New `App\Enums\QuizType` (`practice`/`exam`) -- unlike `QuestionTypeEnum`, there's no later task un-bundling quiz "types" into separate behaviour classes, so nothing blocks defining it now. `settings` is a placeholder json column; its actual shape is the "exam settings" task's job later in this phase
- [x] Implement type: `multiple_choice`. First real `QuestionType` implementation, first entry in `QuestionTypeRegistry::MAP`. `payload`: `{"options": [{"id", "text"}, ...], "correct_option_id"}`; `payloadRules()` validates structure only (min 2 options, required id/text) -- doesn't cross-check `correct_option_id` actually matches one of the options' ids, since that would need a `DataAwareRule`/closure-based rule for a schema layer with no consuming FormRequest yet, and `grade()` is already defensive about it (an unmatched id just grades incorrect, doesn't error). `grade()` treats any non-string answer (missing, wrong type, array) as incorrect rather than throwing -- an untrusted "answer" coming from a cursist's submission should never be able to crash grading. Two Vue components (`Editor`/`Player`) + `resources/js/types/questions.ts` (new file, grows one interface per type same as `blocks.ts` did, except incrementally across many commits instead of one)
- [x] Implement type: `multiple_response` (with partial credit). `payload`: `{"options", "correct_option_ids": [...]}`; `answer`: `list<string>`. Partial credit is Moodle-style: each of the N correct options is worth `points/N`; each correctly-selected option adds its share, each incorrectly-selected one subtracts it, clamped to `[0, points]` so selecting everything never scores worse than selecting nothing. Same "malformed answer -> incorrect, never throw" defensiveness as `multiple_choice`
- [x] Implement type: `true_false`. `payload`: `{"correct_answer": bool}`; `answer`: bool. Same shape and defensiveness as the previous two types, just the simplest possible payload/grading
- [x] Implement type: `open_short` (keyword/regex matching). `payload`: `{"match_mode": "exact"|"contains"|"regex", "case_sensitive": bool, "acceptable_answers": [string, ...]}` -- one list serves all three modes (literal strings for exact/contains, full regex patterns with their own delimiters for regex mode), rather than a separate field per mode. A malformed admin-authored regex is suppressed with `@preg_match` (documented as deliberate, narrow, not a stand-in for validating the pattern) rather than allowed to crash grading -- a bad pattern should never be able to take down every cursist's attempt at that question
- [x] Implement type: `essay` (manual grading + rubric). `payload`: `{"rubric": [{"criterion","points"}, ...], "min_words", "max_words"}`. First manual-grading type -- `isAutoGradable()` is `false`, and `grade()` always returns `GradeResult::pendingManualGrading()` regardless of the submitted answer's shape (even blank/malformed), since only a human can score an essay; what a grading pipeline does with an ungraded/empty submission at finalization time is the grading service's business rule, a later task, not this type's
- [x] Implement type: `matching`. `payload`: `{"pairs": [{"id", "left", "right"}, ...]}` -- one flat list, no separate cross-referenced `right_options` list; assumes `right` values are unique per question (the editor's job to enforce, not this schema-level type). `answer`: `Record<pair id, chosen right text>`. Partial credit: `points * (correct pairs / total pairs)`. Player uses a `<select>` per left prompt rather than drag & drop -- fully keyboard-accessible natively, and `matching` isn't one of the drag/hotspot types the later "keyboard + ARIA alternative" task specifically targets
- [x] Implement type: `ordering`. `payload`: `{"items": [{"id","text"}, ...]}` -- array order is the correct order. `answer`: `list<string>` of ids in submitted order. Partial credit is purely positional (`points * items in their correct index / total`), not edit-distance-based, so a single adjacent swap only costs 2 of N positions. Editor and player both use up/down move buttons rather than drag & drop -- natively keyboard-accessible, same reasoning as `matching`'s `<select>` choice
- [x] Implement type: `fill_in_blank` (cloze). `payload`: `{"template": "...{{1}}...", "blanks": [{"id","acceptable_answers","case_sensitive"}, ...]}`. How `{{id}}` markers in `template` map to `blanks[].id` and render as inline inputs is entirely the player component's job (regex-split into text/blank segments, never `v-html`) -- `grade()` only cares about the blanks list and submitted values, same partial-credit-per-item shape as `matching`/`ordering`
- [x] Implement type: `dropdown_in_text`. Same template/blanks shape as `fill_in_blank`, but each blank carries fixed `options` + a `correct_option` and grading is an exact string match rather than fuzzy/case-insensitive -- a dropdown selection has no typing variance to account for. Editor/player reuse the identical template-segment-splitting approach from `fill_in_blank`'s player, swapping the inline `<input>` for a `<select>`
- [x] Implement type: `numeric` (with tolerance). `payload`: `{"correct_answer", "tolerance"}` -- absolute `±tolerance` margin, defaulting to 0 (exact match) when omitted. Accepts a numeric string as well as an actual number for `answer`, since form inputs commonly submit numbers as strings; anything else (non-numeric string, array, null) grades incorrect rather than erroring
- [x] Implement type: `hotspot_image`. `payload`: `{"image_url", "regions": [{"id","x","y","width","height","label"}, ...], "correct_region_ids"}` -- coordinates are percentages of the image (resolution-independent), and `label` is `required` for every region specifically so the player can offer a real keyboard alternative. Same Moodle-style multi-select partial credit as `multiple_response`, kept as its own copy rather than extracted into a shared helper -- both algorithms are short enough that sharing would add more indirection than it saves. Player's actual answer-submission mechanism is a labeled `<fieldset>` of checkboxes (works with zero mouse/image interaction); the image + click overlay on top is a visual convenience for mouse/touch users, not a second, unequal path -- satisfies CLAUDE.md §5's "a type is not done without [a keyboard alternative]" for this type now, rather than deferring all keyboard/ARIA work to the later consolidated task
- [x] Implement type: `drag_drop_image`. `payload`: `{"image_url", "drop_zones": [{"id","x","y","width","height","label"}, ...], "draggable_items": [{"id","text"}, ...], "correct_placements": Record<zone id, item id>}`. Same accessibility approach as `hotspot_image`: the player's real answer mechanism is a `<select>` per labeled drop zone listing the draggable items by text (the same pattern `matching` already uses), fully keyboard-operable; the drag gesture on the image is a visual convenience layered on top, not the only path. Per-zone partial credit, same shape as `matching`/`fill_in_blank`/`dropdown_in_text`
- [x] Implement type: `likert` (non-scored). `payload`: `{"scale": [{"value","label"}, ...]}`. First genuinely non-scored type -- needed a new `GradeResult::notApplicable()` (pointsAwarded null, not manual, `pointsPossible` 0.0), distinct from `pendingManualGrading()`: essay's "pending" means a human will grade it eventually, likert's "not applicable" means nothing ever will. `GradeResult::isCorrect()` generalized to return null whenever `pointsAwarded` is null (was previously gated only on `requiresManualGrading`), which covers both states correctly without adding a second flag -- existing tests for `pendingManualGrading()` still pass since its `pointsAwarded` was already null
- [x] Implement type: `file_upload`. `payload`: `{"allowed_mime_types", "max_size_bytes"}` -- describes upload constraints only; what the submitted `answer` actually references (a `Media` row, tied to an attempt) is the `quiz_attempts`/`question_answers` task's job to define, not this schema-level type. Second manual-grading type (`pendingManualGrading()` always, same as `essay`). Player is a plain file input with no upload wiring yet -- there's no attempt/submission flow to wire it to until that later task exists. **All 14 question types are now implemented and registered in `QuestionTypeRegistry::MAP`.** `QuestionTypeRegistryTest`'s per-commit "throws for the next unregistered type" test no longer has a real case to target now that the map is complete -- replaced with a positive test asserting every case resolves to a `QuestionType` whose `key()` matches
- [x] Keyboard + ARIA alternative for every drag/hotspot type — a type is not done without it. Both `hotspot_image` and `drag_drop_image` already built their keyboard path as part of their own implementation task, not deferred here (CLAUDE.md's own wording -- "a type is not done without it" -- read as a same-commit requirement, not a follow-up). In both, the actual answer-submission mechanism is keyboard-native (a labeled checkbox `<fieldset>` for hotspot regions, a labeled `<select>` per drop zone for drag/drop, mirroring `matching`'s already-keyboard-native `<select>` approach) and the mouse-oriented image overlay/drag gesture is a visual convenience layered on top, not a separate unequal path. This task's remaining, actual work: added the missing `drag_drop_image` "every drop zone requires a label" payload test (`hotspot_image` already had its equivalent), so both types now have matching, explicit test coverage proving the schema itself enforces what the keyboard alternative depends on. No browser-based a11y tooling (axe, screen-reader testing) exists in this repo and none was added -- out of reach without a live browser session, which CLAUDE.md §0 rule 8 rules out; verification here is schema + component-review level only
- [x] Question bank per reseller, reusable across quizzes. `questions.quiz_id` (a required single-parent FK) structurally couldn't represent "the same question reused across multiple quizzes" -- replaced with a `quiz_questions` pivot (`quiz_id`, `question_id`, `order`, unique pair) and `Question` gained the same mixed-ownership shape as `Course`/`CourseCategory`/`Media` (nullable `reseller_id`: null = platform-shared bank question, set = one reseller's own; no `TenantScoped`, new `EloquentQuestionRepository::visibleToCurrentReseller()`, added to `tenancy:audit`'s `EXEMPT` list, custom visibility test since the standard `assertTenantIsolated()` helper assumes single ownership). `questions.order` was also dropped -- it was "position within the one quiz", which has no single answer once a question can belong to several; ordering now lives on `quiz_questions.order`, per-attachment. `Quiz::questions()`/`Question::quizzes()` are now `BelongsToMany` through the pivot (was `HasMany`/`BelongsTo`). New `App\Actions\Questions\AttachQuestionToQuiz` (defaults `order` to the current count, `syncWithoutDetaching` so re-attaching is idempotent) and `DetachQuestionFromQuiz`. Also cast `Question.type` to `QuestionTypeEnum` now that all 14 types exist -- the "next task's job" placeholder from the schema task's own docblock no longer applied. Updated `QuizFactory`-based tests that used the old direct `quiz_id` FK (`QuizTest`, `QuestionTest`) to attach via the pivot instead
- [x] Exam settings: time limit, attempt limit, pass threshold, retake rules, question pool size. Firms up `quizzes.settings` (a placeholder json column since the schema task) into a typed shape: new `App\DataTransferObjects\Quizzes\QuizSettingsData` (`timeLimitMinutes`, `attemptLimit`, `passThresholdPercent`, `cooldownMinutesBetweenAttempts`, `questionPoolSize`) plus `App\Casts\QuizSettingsCast` doing the array/JSON <-> DTO conversion, same split `MoneyCast`/`Money` already established. "Retake rules" is expressed as `attemptLimit` (null = unlimited) + `cooldownMinutesBetweenAttempts` (null = no cooldown) rather than a separate enum -- a distinct "retake policy" concept alongside attempt count would risk conflicting semantics for no real gain. `set()` returns a JSON-encoded string, not a raw array -- unlike Laravel's own multi-column custom-cast example (an Address split across two real columns), `settings` is one `json` column, so the bound value must already be encoded. `passThresholdPercent`'s 0-100 range isn't validated here -- that's a FormRequest's job once a consuming route exists, consistent with every other payload/settings shape built so far in this phase
- [x] Question and answer randomization. **Question side**: new `App\Services\Quizzes\QuizQuestionRandomizer::questionsFor(Quiz $quiz)` -- samples down to `settings->questionPoolSize` when set and smaller than the attached pool (never crashes by requesting more than available), then shuffles if `settings->shuffleQuestions` is true (a new boolean added to `QuizSettingsData`/`QuizSettingsCast`, independent of pool size -- fixed order with a smaller pool, or the full set shuffled, are both real, independent configurations). **Answer side**: deliberately left to the player components at render time, not built here -- every one of the 14 question types grades by option id, never by position (verified across all their `grade()` implementations built earlier in this phase), so client-side shuffling of displayed option order can never affect correctness. Building a generic server-side answer-shuffling mechanism now, with no consuming quiz-taking page to wire it into yet, would be speculative
- [x] `quiz_attempts` + `question_answers`, full answer history retained. `quiz_attempts`: `quiz_id`, `user_id`, `attempt_number` (unique per quiz+user, enforces attempt sequencing at the DB level), `started_at`/`submitted_at`, `score`/`max_score`/`passed` (all nullable until graded). `question_answers`: `quiz_attempt_id`, `question_id`, `order` (this attempt's actual question position -- can't live on `quiz_questions.order` since `QuizQuestionRandomizer` may shuffle per attempt), `answer` (json, set once at submission and never mutated afterward), `points_awarded`/`is_correct`/`requires_manual_grading`/`feedback`/`graded_at` (the only fields a regrade updates), unique per `(quiz_attempt_id, question_id)`. "Full answer history retained" is satisfied by never overwriting `answer` after submission plus both tables using soft deletes (CLAUDE.md §0) rather than by snapshotting the full question content -- same minimal-versioning judgment call already made for `Course` publishing, since nothing yet needs to reconstruct a question's exact historical wording. `points_possible` IS snapshotted per-answer though, since a bank question's `points` can legitimately change after this attempt was graded, and the historical record should reflect what was actually possible at the time. No `reseller_id` on either table -- same "ownership inherited transitively" pattern as `Quiz`/`Question`, this time following the attempting `User`'s own reseller rather than a column here
- [x] Grading service, auto + manual, with regrade capability. New `App\Services\Grading\GradingService`. "Regrade capability" isn't a separate method: `gradeAnswer()` is itself safely re-callable for auto-graded answers (e.g. after fixing a bank question's payload) since it always re-runs the resolved `QuestionType::grade()`; `gradeManually()` is both "grade" and "regrade" for manual types. The one real safeguard: once an answer is classified `requires_manual_grading` (its type isn't auto-gradable), `gradeAnswer()` short-circuits and does nothing on every later call -- without that, a scheduled/accidental `gradeAnswer()` re-run would call the manual type's `grade()` again, which always reports "pending", silently wiping out any score a human already recorded via `gradeManually()`. `gradeAttempt()` grades every answer then aggregates `score`/`max_score`/`passed`; `passed` stays `null` (not `false`) whenever a manual answer is still ungraded, or the quiz has no `passThresholdPercent` (a practice quiz has no pass/fail concept at all) -- both are genuinely "not decided yet", not "failed"
- [x] Feedback per answer and per question, shown per configurable rules. **Per answer**: `question_answers.feedback`, already added with the schema task, settable per-answer via `GradingService::gradeManually()`. **Per question**: canned feedback now lives in `Question.settings` (not `payload`, which is type-specific) as `{"feedback": {"correct": "...", "incorrect": "..."}}` -- a cross-cutting convention every type can carry regardless of its own payload shape. `GradingService::gradeAnswer()` applies it automatically when the type's own `GradeResult` didn't already supply feedback (only when `isCorrect` is non-null -- pending/not-applicable results get none). **Configurable rules**: new `App\Enums\FeedbackVisibility` (`immediate` / `after_submission` / `never`) added to `QuizSettingsData` (default `after_submission`), and a new `App\Services\Grading\AnswerFeedbackPresenter::feedbackFor()` that withholds an already-graded answer's feedback until the configured moment -- e.g. `after_submission` hides it on an in-progress attempt even though the underlying `question_answers.feedback` column is already populated
- [x] Unit tests per type: correct / incorrect / partial / empty. Already satisfied incrementally -- each of the 14 `Implement type: X` tasks above built its own dedicated test file covering exactly this (correct, incorrect, partial where the type supports it, and empty/malformed answers graded defensively rather than erroring), rather than deferred to one final catch-all task. `multiple_choice`/`true_false` are binary (no partial credit exists for them); `essay`/`file_upload` always report pending (manual); `likert` always reports not-applicable (non-scored) -- each verified as its own explicit test rather than a gap
- [ ] **Gap discovered in Phase 8** (Completion notification task): `quiz_attempts`/`question_answers` (schema above) and `GradingService`/`AnswerFeedbackPresenter` (grading/feedback above) exist, but there is no Action anywhere in the codebase that actually *submits* a quiz attempt (sets `QuizAttempt::submitted_at`, triggers grading, persists `question_answers` from a cursist's in-progress answers). `submitted_at` is currently only ever set via factory states in tests. This means the two exam-based course completion rules (`pass_exam`, `minimum_score`) have no real path to ever become true outside a test -- only `all_lessons` does, via `MarkBlockCompleted`. Needs a `SubmitQuizAttempt` (or similar) Action plus the player-facing route/controller/page to reach it, none of which exist yet. Flagging here rather than building inline -- a full quiz-taking flow is its own task, not something to improvise as a side effect of a notifications task.

---

## Phase 5 — Enrollment, progress, certificates

- [x] `course_assignments` table: user, course, assigned_by, assigned_at, first_opened_at, revoked_at, billing state. This is the billable event per CLAUDE.md §11, and unlike `Course`/`Question`/`Media`, a `CourseAssignment` always belongs to exactly one reseller (a cursist and their assignments are never platform-shared) -- so it uses `TenantScoped` normally, not the mixed-ownership repository pattern, with a full `tests/Tenancy` isolation test. `first_opened_at`/`revoked_at` are both nullable timestamps stored independently, exactly matching §11's revocation rule wording ("free within 14 days *if never opened*") -- the actual free-vs-billed decision logic and price calculation (`AssignmentPricingService`, `AssignCourseToCursist`, CLAUDE.md §3a's own worked example for this exact table) is deliberately not built yet: schema + model only, matching this phase's own task breakdown (the next lines are "Assignment UI" and the rest of enrollment, which is where the actual assignment-creation flow belongs). New `App\Enums\AssignmentBillingState` (`pending`/`billed`/`waived`) tracks invoicing status only -- real invoice generation is Phase 8's job
- [ ] Assignment UI: individual, bulk, by group -- **individual and bulk backend done** (see note); "by group" deliberately deferred to the very next task, since `groups` doesn't exist yet and assigning by group has nothing to attach to until it does. Built the backend capability (Actions/Service), not a Vue page: no admin panel exists yet for it to live in (Phase 7), same recurring "no consuming page yet" situation as most of Phase 3/4's schema-level work. `course_assignments` was missing a `price_cents` column (an oversight from its own task -- added here via a follow-up migration, same pattern as `convert_questions_to_reusable_bank`), since billing needs the assignment's price snapshotted at assignment time, same reasoning as `question_answers.points_possible`. New `App\Services\Billing\AssignmentPricingService` implements CLAUDE.md §11's pricing rules exactly (15%/€3 floor for reseller courses, fixed platform price for catalog, manual floor override never auto-detected -- a human enforces it, this service just applies whatever's already on the course). `App\Actions\Courses\AssignCourseToCursist` deliberately does NOT match CLAUDE.md §3a's illustrative `AssignCourseData{courseId, ...}` DTO shape -- it takes the `Course` model directly instead, since resolving a `courseId` would need a `CourseRepository::find()` pass-through method, which CLAUDE.md's own repository rules explicitly ban; the more specific rule wins over the illustrative sketch. `BulkAssignCourseToCursists` wraps `AssignCourseToCursist` in a loop inside one transaction, reusing it rather than duplicating the pricing/creation logic. Task stays unchecked until "by group" and an actual UI exist
- [x] `groups` per resellerklant, group-based assignment. `groups` + `group_members` pivot; `Group` is single-owner (via its `resellerklant`) so it uses `TenantScoped` normally, same as `CourseAssignment`, with a full isolation test. New `App\Actions\Courses\AssignCourseToGroup` reuses `BulkAssignCourseToCursists` against the group's member ids, closing out the "by group" part of the previous "Assignment UI" task (still unchecked overall, since the actual UI still doesn't exist). **Bug found and fixed while writing this task's tests**: `User::resellerKlant()` and `UserInvite::resellerKlant()` (both pre-existing, from Phase 1) relied on Eloquent's default FK inference, which guesses `reseller_klant_id` from the relation name -- the real column (matching the `resellerklanten` table's own spelling) is `resellerklant_id`, no underscore. Both relations were silently broken (always returned `null`) but never actually exercised by any existing test, so it stayed latent until building `Group`'s identical relation surfaced it. Fixed all three (`User`, `UserInvite`, the new `Group`) with an explicit FK
- [x] Progress tracking per block/lesson/module, resume where left off. Only `block_progress` is a real table (`course_assignment_id`, `block_id`, `last_viewed_at`, `completed_at`) -- lesson/module/course completion is *derived*, not stored redundantly at three levels that could drift out of sync. Writes are two thin Actions (`MarkBlockViewed`, `MarkBlockCompleted`, both `updateOrCreate`-idempotent); reads are `App\Services\Progress\ProgressCalculationService` (`lessonCompletionPercent`/`moduleCompletionPercent`/`courseCompletionPercent`/`resumeBlock`). "Resume where left off" walks module → lesson → block in nested `order` rather than a flat `whereIn`+`orderBy('order')` query -- `order` is only unique within its own parent, so a flat query can't correctly compare positions across different lessons/modules. Progress is keyed off `course_assignment_id`, not just `user_id`+`course_id`, so a repeat assignment (§11: "no reassignment... assigned fresh") naturally starts with zero progress rather than inheriting the previous attempt's
- [x] Completion rules per course (all lessons / pass exam / minimum score). `courses.completion_rule` (typed via `CourseCompletionRuleData`/`CourseCompletionRuleCast`, same split as `QuizSettingsData`/`QuizSettingsCast`) holds exactly CLAUDE.md's three named options -- a fixed enum (`CourseCompletionRuleType`), not an open-ended rules engine. `App\Services\Progress\CourseCompletionChecker::isComplete()` dispatches on the rule type: `all_lessons` delegates to `ProgressCalculationService::courseCompletionPercent()`, `pass_exam`/`minimum_score` both look at the highest-scoring *submitted* attempt at the rule's designated quiz (`pass_exam` checks `QuizAttempt::passed`, `minimum_score` recomputes the percentage against a configurable threshold) -- both default to incomplete, never throw, when no attempt exists yet or the quiz isn't configured
- [x] Deadlines, reminder schedule, overdue flags. `course_assignments` gains `deadline_at` (nullable timestamp) and `reminder_days_before` (json list of day-offsets, default `[7, 1]`). New `App\Services\Progress\AssignmentDeadlineService`: `isOverdue()` is true only when a deadline is set, has passed, the assignment isn't revoked, and the course isn't complete (reuses `CourseCompletionChecker`, so "done late" is never flagged overdue); `reminderOffsetsDueOn()` is a pure lookup answering "which configured offsets land on this date" -- deliberately does **not** send anything or track what's already been sent. The actual mailable, its dispatch, and its own sent-tracking are Phase 8's job (Notifications, billing, GDPR), not enrollment data modeling; this task only had to build the data + the pure calculation a scheduled job will later drive
- [x] Certificates: PDF generation, per-reseller template, unique verification code. Installed `barryvdh/laravel-dompdf` (pure-PHP rendering, no external binary/headless-Chrome dependency to manage on the Ploi/Hetzner VPS, unlike Snappy/Browsershot). New `certificates` table (`course_assignment_id` unique, `verification_code` unique, `issued_at`, `pdf_disk`/`pdf_path`) -- no `reseller_id`, ownership inherited transitively via `CourseAssignment`, same as `BlockProgress`. `App\Actions\Certificates\IssueCertificate` refuses to issue (`CourseAssignmentNotCompleteException`) unless `CourseCompletionChecker::isComplete()` says so, generates a 16-char random `verification_code` (checked for uniqueness against the table, not just assumed), renders `resources/views/certificates/default.blade.php` via the injected `Barryvdh\DomPDF\PDF` service (not the `Pdf` facade -- domain code stays facade-free), and stores the PDF via the same `FilesystemFactory`+private-disk pattern `UploadMedia` already established. **"Per-reseller template"**: one default Blade view styled with that reseller's own `ResellerTheme` (logo, primary color) via `ResellerThemeRepository::findForReseller()` -- not a separate editable template (no certificate template editor was asked for, unlike the reseller mail template editor which got its own dedicated task). Added `resources/lang/{en,nl}/certificates.php` -- the first real use of Laravel's own `lang/` translation files in this codebase (mail views and everything else so far used hardcoded English), since a PDF's static chrome text needs `__()`, unlike course content which goes through `spatie/laravel-translatable` instead
- [x] Public certificate verification page. Genuinely public/unauthenticated -- unlike "Assignment UI" (deferred, no admin panel exists yet to host it), this route needs no admin panel and no tenant context at all, so building the real page now was justified rather than premature. `GET /certificates/verify/{code}` (`CertificateVerificationController`, thin, per §3a) → new `CertificateRepository::findByVerificationCode()` (a repository method even though it's a single query, because the enforced `LayeringTest` doesn't exempt controllers from touching `App\Models` the way §3a's prose about "simple index listings" might suggest -- the architecture test is the actual authority). **Real bug found and fixed**: the eager-loaded `courseAssignment` relation came back `null` for a genuine row, because `CourseAssignment` is `TenantScoped` and this route has no ambient tenant context (a visitor verifying a certificate isn't logged into any reseller) -- fixed with a documented `withoutTenantScope()` on that specific nested eager load, same justification pattern as `ResellerThemeRepository::findForReseller()`. `Inertia/Certificates/Verify.vue` renders a valid/not-found state; `CertificateVerificationResource` is passed as a single instance, which -- confirmed against `Theme.vue`'s existing established convention -- wraps in `{data: ...}`, not bare fields directly. **Also fixed while running the full suite for the first time in a while**: `npm run lint` caught real pre-existing errors from earlier Phase 4 commits (an unused destructured var, unused `defineProps` capture, and a `{{ '{{id}}' }}` literal that broke Vue's template parser in two Editor components) -- all fixed. And `tests/Architecture/LayeringTest` started crashing the full suite with an OOM under PHP's default 128M `memory_limit`, now that docblock-parsing every class under `app/` has grown enough to need more -- raised to 512M in `phpunit.xml`
- [x] Certificate expiry + renewal (drives repetition courses). `courses.certificate_validity_months` (null = never expires) + `certificates.expires_at`, computed once at issuance (`IssueCertificate` now sets it from `issued_at + certificate_validity_months`, never recomputed later). New `App\Services\Certificates\CertificateRenewalService`: `isExpired()`/`isExpiringSoon()` are plain date comparisons; `latestVariantOf()` walks a course's repeat lineage (via `repeats_from_course_id`) back to the root, then finds the newest `variant_year` among its `variants()` -- works whether called with the original course or any older variant. Deliberately does **not** auto-create a renewal assignment -- per §11 "no reassignment," a renewal is just a fresh `AssignCourseToCursist` call against whatever `latestVariantOf()` returns, and deciding *when* to trigger that is a human/admin action or a later notification-flow trigger, not this service's job (same "calculate, don't dispatch" boundary as the deadline/reminder task)
- [x] Repetition courses: `variant_year` + `repeats_from_course_id`, "which editions has this cursist done" report. `variant_year`/`repeats_from_course_id` themselves already existed since the Phase 3 `courses` table task -- this task is the report. New `App\Services\Reports\CursistCourseEditionsReport::forCursist()` walks the full lineage (root + every `variants()`, oldest to newest -- same "walk to root first" approach as `CertificateRenewalService::latestVariantOf()`, kept as its own small copy rather than shared, same reasoning as the `hotspot_image`/`multiple_response` partial-credit duplication earlier) and reports, per edition, whether this specific cursist was ever assigned it and whether they completed it -- including editions they were never assigned, so the report shows the full picture of a course's history, not just this cursist's slice of it. Returns `list<CursistCourseEditionData>`, a genuine DTO (no methods, per CLAUDE.md §9) rather than an ad-hoc array
- [x] "Voorlees" — browser `SpeechSynthesis`: play/pause/stop, speed, per-block and per-question, language follows content locale. Pure frontend, no backend needed at all (browser API only, per CLAUDE.md §1 -- no server-side TTS in v1). New `resources/js/Composables/useVoorlees.ts` wraps `window.speechSynthesis`/`SpeechSynthesisUtterance` with play/pause/resume/stop + a speed setter (changing speed mid-utterance isn't supported by the Web Speech API, so it applies on the next `play()` rather than restarting the current one and jumping the reading position). `localeToBcp47()` maps this app's short locale codes (`available_locales: ["nl", "en"]`) to full BCP-47 tags (`nl-NL`) for better voice matching, falling back to the bare code for anything unmapped. New `resources/js/Components/VoorleesControls.vue` is a standalone, reusable play/pause/stop/speed control bar -- like the rest of this phase's block/question player components, no consuming lesson/quiz player page exists yet for it to be dropped into, so it stays a self-contained component. Per-block/per-question wiring is deferred to whichever future task builds that player page, since the actual capability (this composable + component) is what this task asked for, not the integration into pages that don't exist yet
- [x] Cursist dashboard: assigned, in progress, completed, deadlines. Genuinely real, needed for every authenticated user (not admin-only), so unlike "Assignment UI" this got a real page. New `CourseAssignmentRepository::forUser()` (excludes revoked assignments), `App\Services\Progress\CursistDashboardService` combines it with `ProgressCalculationService`/`CourseCompletionChecker`/`AssignmentDeadlineService` (all already built earlier this phase) into `CursistAssignmentRowData` rows, deriving a new `AssignmentStatus` (not_started/in_progress/completed -- from `first_opened_at` + completion, not stored on `CourseAssignment` itself). Rows are passed as a DTO array directly, no `Http\Resource` wrapper -- CLAUDE.md §3a names a DTO as the other sanctioned way to shape Inertia props, and a plain readonly DTO's public properties already `json_encode()` correctly with no model attributes to leak. `GET /dashboard`, `CursistDashboardController`, `Dashboard.vue` grouped by status. **This completes Phase 5.**

---

## Phase 6 — Access control matrix

- [x] Platform → reseller: grant catalog course or whole category access. Added nullable `courses.course_category_id` (deferred from Phase 3 until a real consumer needed it -- this is that consumer), `course_access_grants` (reseller_id, nullable course_id/course_category_id -- exactly one, enforced by a `saving` guard throwing `InvalidCourseAccessGrantException`, mirroring `Quiz`'s module/lesson pattern). `App\Models\CourseAccessGrant` is single-owner `TenantScoped` (the reseller being granted access always owns the grant row). `App\Contracts\Repositories\CourseAccessGrantRepository`/`EloquentCourseAccessGrantRepository::activeGrantsForReseller()` explicitly bypasses the tenant scope (documented) since callers -- platform admins explaining another reseller's access -- have no ambient tenant context of their own. `App\Services\Access\CourseAccessChecker::explain()`/`resellerHasAccess()` computes effective access dynamically (own course, or an active direct grant, or an active grant on the course's *current* `course_category_id`) rather than snapshotting anything. `App\Actions\Access\GrantCourseAccess`/`RevokeCourseAccess` (revoke sets `revoked_at`, never deletes).
- [x] Reseller → resellerklant: which courses a klant may assign. `resellerklant_course_grants` (reseller_id, resellerklant_id, course_id -- course-only, no category level at this tier per this task's own wording). `App\Models\ResellerKlantCourseGrant` is single-owner `TenantScoped`, table name explicit (`resellerklant_course_grants`, matching `ResellerKlant`'s own no-underscore spelling, not Eloquent's snake_case guess from the class name). `App\Services\Access\ResellerKlantCourseAccessChecker` is layered on top of `CourseAccessChecker`: the reseller-level check always runs first, so a resellerklant-level grant can never widen access beyond what its reseller has (proven by `ResellerKlantCourseAccessCheckerTest`'s "never lets a resellerklant grant widen access beyond a platform grant the reseller lost" case). `App\Actions\Access\GrantCourseToResellerKlant` enforces the same invariant on write, not just read, throwing `ResellerLacksCourseAccessException` if the reseller itself lacks access -- an invalid grant can never be created. `RevokeCourseFromResellerKlant` mirrors `RevokeCourseAccess` (sets `revoked_at`, never deletes).
- [x] Klant → cursist: actual assignment (Phase 5). Already built in Phase 5 -- `App\Actions\Courses\AssignCourseToCursist`/`BulkAssignCourseToCursists`/`AssignCourseToGroup`. Checkbox recorded here since this line is the matrix's third level.
- [x] Category-level grants that cascade to new courses added later. Satisfied by construction: `CourseAccessChecker` reads `Course::course_category_id` live on every check rather than snapshotting category membership at grant time. Proven by `CourseAccessCheckerTest`'s "cascades a category grant to a course added to the category after the grant was made" case, which creates the course *after* the grant already exists.
- [x] Access changes are audit-logged with before/after. `CourseAccessGrant`/`ResellerKlantCourseGrant` both use the same `HasAuditLog` trait (spatie/laravel-activitylog) as every other model in this codebase -- generic behavior already covered by `HasAuditLogTest`, not re-tested per model.
- [x] Revoking access does not delete progress or certificates. True by construction -- neither grant table has any relationship to `CourseAssignment`/`BlockProgress`/`Certificate` at all. Proven explicitly by `RevokeCourseAccessTest`'s "does not touch existing assignments, progress, or certificates when access is revoked" case.
- [x] Effective-access debug view: "why can this user see this course?". Backend calculation only -- `CourseAccessChecker::explain()` (platform → reseller, `CourseAccessReason`) and `ResellerKlantCourseAccessChecker::explain()` (reseller → resellerklant, `ResellerKlantCourseAccessReason`), each returning a DTO with the reason. No consuming admin page yet, same "no consuming page" pattern used for admin-only features earlier in this codebase; the real page is Phase 7's admin panel.
- [x] Tests for every level of the matrix. Level 1 (platform → reseller): `CourseAccessGrantTest`, `CourseAccessGrantIsolationTest`, `GrantCourseAccessTest`, `RevokeCourseAccessTest`, `CourseAccessCheckerTest`, `EloquentCourseAccessGrantRepositoryTest`. Level 2 (reseller → resellerklant): `ResellerKlantCourseGrantTest`, `ResellerKlantCourseGrantIsolationTest`, `GrantCourseToResellerKlantTest`, `RevokeCourseFromResellerKlantTest`, `ResellerKlantCourseAccessCheckerTest`, `EloquentResellerKlantCourseGrantRepositoryTest`. Level 3 (klant → cursist) already covered in Phase 5. **This completes Phase 6.**

---

## Phase 7 — Admin panel & reporting

- [x] Platform admin dashboard: resellers, users, courses, revenue, storage. `App\Contracts\Repositories\PlatformDashboardRepository`/`EloquentPlatformDashboardRepository` (reseller/user/course counts, revenue by `AssignmentBillingState` -- explicitly bypasses the tenant scope, documented, since a platform-wide total spans every reseller by definition). Added `MediaRepository::totalBytesAcrossPlatform()` for the storage figure. `App\Services\Reporting\PlatformDashboardService::snapshot()` composes those with `StorageMetering::includedBytes()` (platform total capacity = per-reseller allowance × reseller count) into `PlatformDashboardData`, passed straight to Inertia as a DTO (no Resource wrapper needed). `PlatformDashboardController` authorizes via the existing `ResellerPolicy::viewAny` (`$this->authorize('viewAny', 'App\Models\Reseller')`, string form -- controllers can't import `App\Models` directly, see `LayeringTest`) since viewing platform-wide stats is exactly the same access boundary as viewing resellers themselves; no new Policy/Gate needed. `GET /admin/platform`, `Admin/Platform/Dashboard.vue`. **Fixed a latent bug found while building this**: `AdminLayout.vue`'s `<slot name="nav">` was dead code -- Inertia's persistent-layout wrapping (`defineOptions({ layout })`) only ever forwards a single default slot from page to layout, so a page can never actually fill a named slot on it. Nothing had exercised `AdminLayout` before this task. Replaced with a nav list rendered directly in the layout.
- [x] Reseller admin dashboard: klanten, cursisten, assignments, spend. `App\Contracts\Repositories\ResellerDashboardRepository`/`EloquentResellerDashboardRepository` -- scoped to the current reseller via the ambient `TenantContext` (never bypasses the tenant scope, unlike the platform version, since a reseller admin only ever sees their own numbers). "Cursisten" = users with `resellerklant_id` set, excluding the reseller's own staff. `App\Services\Reporting\ResellerDashboardService::snapshot()`, `ResellerDashboardController` authorizes via the existing `ResellerKlantPolicy::viewAny` (same access boundary as `/klanten`). `GET /admin/reseller`, `Admin/Reseller/Dashboard.vue`. New `ResellerAdminLayout.vue` (separate from the platform `AdminLayout.vue` -- different nav, different audience) rather than trying to make one layout's nav conditional on the viewer's role.
- [x] Klant dashboard: cursisten, progress, completions. Third dashboard level, self-service: a klant-admin/klant-manager views their own klant's cursisten and each one's assigned/in-progress/completed counts. `ResellerKlant::cursisten()` `HasMany<User>` (via `resellerklant_id`) added. `App\Services\Reporting\KlantDashboardService::forKlant()` reuses `CursistDashboardService::forUser()` per cursist (already built in Phase 5) rather than duplicating its status/progress calculation -- only aggregates those rows into a summary. New `ResellerKlantRepository::findOwnKlant(User $user)` resolves "the current user's own klant" so the controller never has to touch `$user->resellerklant_id` itself (controllers can't import `App\Models` directly). New `ResellerKlantPolicy::view()` -- unlike this policy's other placeholder methods (written before roles existed), this one uses real spatie roles from the start: reseller staff manage every klant in their reseller, but a klant-admin/klant-manager may only view their *own* klant, and a plain cursist may not view the aggregate dashboard at all (same role-check pattern as `UserPolicy::impersonate()`). `GET /admin/klant`, `Admin/Klant/Dashboard.vue`, new `KlantAdminLayout.vue` (third distinct nav audience, same reasoning as the reseller one).
- [x] Generic filter/sort/search layer reusable across every index (query builder + saved filters). `App\DataTransferObjects\Filtering\FilterRequestData::fromRequest()` parses `?search=&sort=&direction=&filter[x]=y` once; `App\DataTransferObjects\Filtering\FilterableSpec` is the per-index allowlist (searchable columns, allowed sorts/filters, default sort); `App\Support\Filtering\QueryFilterApplier::apply()` applies one against an Eloquent builder -- deliberately outside the Actions/Repositories/Services layering (a generic, stateless query helper Repositories use internally, not a domain read itself; Repositories still never leak a Builder to their own callers). Saved filters: `saved_filters` table + `App\Models\SavedFilter` (scoped by `user_id` only, not `TenantScoped` -- a personal preference, not a reseller-owned resource; platform staff have no `reseller_id` to scope by anyway), `App\Enums\FilterableResource` (the closed set of indexes this applies to, matching the next task's literal list), `App\Actions\Filtering\CreateSavedFilter`/`DeleteSavedFilter`, `SavedFilterRepository`, `SavedFilterPolicy`, `StoreSavedFilterRequest`. Deliberately backend-only, no controller/routes/page yet -- same "no consuming page" pattern as Assignment UI in Phase 5; the next task is the real consumer.
- [ ] Filterable index for: users, resellers, klanten, courses, assignments, attempts, invoices, activity. Split into sub-tasks (bigger than one commit, CLAUDE.md §0 rule 4) -- one index per commit, each wired through FilterRequestData/FilterableSpec/QueryFilterApplier from the task above:
  - [x] Filterable index: resellers (platform admin). `ResellerRepository::paginate(FilterRequestData)` (search name/slug, sort name/status/created_at, filter by status, default-sorted by name) using `QueryFilterApplier` internally. `QueryFilterApplier` itself needed a fix: its generic was originally declared at the class level (`@template TModel` on the class), which broke since one shared instance is injected everywhere via DI and can't be bound to a single model -- moved to a method-level `@template` on `apply()` instead, generic per-call. `ResellerResource`, `ResellerIndexController` (auth via the same `ResellerPolicy::viewAny` as the platform dashboard), `GET /admin/platform/resellers`, `Admin/Platform/Resellers/Index.vue`. New reusable pieces for every remaining index in this task: `resources/js/types/filtering.d.ts` (`PaginatedCollection<T>`, `FilterQuery`) and `resources/js/Composables/useIndexFilters.ts` (search debounce, sort-column click, filter changes -- all round-tripped through the URL query string so pages stay bookmarkable).
  - [x] Filterable index: users (platform admin -- every user across every reseller). New `UserRepository`/`EloquentUserRepository::paginate()` (search name/email, sort name/email/created_at, filter by `reseller_id`/`platform_role`, eager-loads `reseller` for display) -- deliberately not tenant-scoped, this is the one place a platform admin needs to see every user across every reseller plus platform staff. Same `ResellerPolicy::viewAny` auth boundary as the resellers index (not `UserPolicy`, which is reseller-scoped and explicitly excludes platform staff -- the wrong shape for this page). `UserResource`, `UserIndexController`, `GET /admin/platform/users`, `Admin/Platform/Users/Index.vue`.
  - [x] Filterable index: klanten (reseller admin -- already has a basic index at `/klanten`; add filter/sort/search to it rather than building a second one). Changed `ResellerKlantRepository::paginate()`'s signature from a raw `?string $search` to `FilterRequestData` (search name, sort name/created_at, default-sorted by name) -- updated both call sites (`ResellerKlantController`, and `InvitesController`'s unrelated `klanten` prop for the invite form, which now passes an explicit empty `FilterRequestData` instead of relying on a default). `Klanten/Index.vue` gets a search input and a sortable "Naam" column via `useIndexFilters`, kept in the page's existing Dutch, consistent with its sibling labels ("Toevoegen", "Verwijderen").
  - [x] Filterable index: courses (reseller admin -- catalog + own, same scope as `CourseRepository::visibleToCurrentReseller()`). New `CourseRepository::paginate()` (search title, sort status/created_at, filter by status) reuses that method's existing visibility scope rather than layering `CourseAccessChecker` on top, on reflection: `CourseAccessChecker` governs assignment *eligibility* (can this catalog course be granted to a resellerklant), not browsing visibility, and every sibling mixed-ownership repository (Media, Question, CourseCategory) already treats "platform + own" as the one visibility rule -- narrowing just this index would be inconsistent with that established pattern, not a real requirement of the task. `CourseIndexResource`, `CourseIndexController` (auth via the same `ResellerKlantPolicy::viewAny` as the reseller dashboard, since this is a reseller-scoped index, not platform-wide), `GET /admin/reseller/courses`, `Admin/Reseller/Courses/Index.vue`.
  - [x] Filterable index: assignments (reseller admin). `CourseAssignmentRepository::paginate()` -- `CourseAssignment` is `TenantScoped`, so it's already scoped to the current reseller with no explicit filtering needed; sort by `assigned_at`/`deadline_at`/`billing_state`, filter by `billing_state`. No free-text search: course title/cursist name live on related tables, out of scope for `QueryFilterApplier`'s single-table design. `CourseAssignmentIndexResource` (guards `price_cents` being null -- the factory default leaves it unset, and hit the same nullsafe-with-`??` Larastan false positive documented earlier this session; fixed with the same explicit `=== null ? … : …` ternary), `CourseAssignmentIndexController`, `GET /admin/reseller/assignments`, `Admin/Reseller/Assignments/Index.vue`.
  - [x] Filterable index: attempts (`QuizAttempt`, reseller admin). New `QuizAttemptRepository`/`EloquentQuizAttemptRepository::paginate()` -- `QuizAttempt` has no `reseller_id` of its own (ownership follows the attempting user transitively, same as `Quiz`/`Question`), so scoped via `whereHas('user', ...)` against the current reseller instead of a direct column filter. Sort by `started_at`/`submitted_at`/`score`, filter by `passed`. `QuizAttemptIndexResource` (quiz has no title of its own, so exposes `quiz_type` -- practice/exam -- as the closest identifying field), `QuizAttemptIndexController`, `GET /admin/reseller/attempts`, `Admin/Reseller/Attempts/Index.vue`.
  - [x] Filterable index: activity (`spatie/laravel-activitylog`'s `Activity` model, platform admin). New `ActivityLogRepository`/`EloquentActivityLogRepository::paginate()` -- platform-wide, spanning every reseller, since `Activity` has no `reseller_id` of its own (it records actions against every kind of subject, tenant-owned or not); search `description`, sort `created_at`/`event`, filter by `event`/`log_name`/`subject_type`. `ActivityLogResource` resolves the causer's name when it's a `User` (a morph relation, could in principle be any causer type). `ActivityLogIndexController`, `GET /admin/platform/activity`. **This completes the "filterable index" task except invoices, which stays blocked on Phase 8.**
- [x] Activity log viewer: filter by actor, subject, action, date range, reseller. Extended the filterable activity index above (rather than building a second page) with the remaining filters this task names: `causer_id` (actor) and `subject_id` added to `QueryFilterApplier`'s plain-equality `allowedFilters`; `date_from`/`date_to` and `reseller_id` are not plain column equality, so `EloquentActivityLogRepository::paginate()` pulls those three keys out of the same `?filter[x]=y` bag before the rest goes through the generic applier -- `date_from`/`date_to` become `created_at` range clauses, and `reseller_id` filters via `whereHasMorph('causer', [User::class], ...)` since `Activity` has no `reseller_id` column and the causer may not even be a `User`. `Admin/Platform/Activity/Index.vue` gets actor/subject-type/action/reseller/date-range inputs alongside the existing search box.
- [x] Per-user detail page: full timeline of everything they did. New `UserRepository::findById()`, `ActivityLogRepository::timelineForUser()` (matches every `Activity` row where the user is either `causer` or `subject`, ordered newest-first). `UserDetailController`, `GET /admin/platform/users/{user}`, `Admin/Platform/Users/Show.vue` (linked from the users index). Same `ResellerPolicy::viewAny` platform-wide access boundary as the rest of the platform admin area.
- [x] Impersonation entry points from user detail pages. The controller computes `canImpersonate` via the existing `UserPolicy::impersonate()` (viewer-dependent, so kept out of `UserResource` -- that policy's own hierarchy already restricts platform staff to super-admins only, which the page respects rather than working around). The detail page's reason field + submit form was this feature's genuinely first frontend consumer -- `ImpersonationController`/`StartImpersonation` already existed (Phase 1), but nothing in the UI could reach it before now.
- [ ] Exports: CSV/XLSX for every index, queued for large sets, expiring signed download links. Split into sub-tasks (bigger than one commit, CLAUDE.md §0 rule 4):
  - [x] Shared `FilterablePaginator` contract across every filterable-index repository (`ResellerRepository`, `UserRepository`, `ResellerKlantRepository`, `CourseRepository`, `CourseAssignmentRepository`, `QuizAttemptRepository`, `ActivityLogRepository`). They already all had the exact same `paginate(FilterRequestData, int): LengthAwarePaginator` signature, discovered emergently across the "filterable index" sub-tasks -- formalized now because the export job genuinely needs to call `paginate()` generically across whichever repository `App\Enums\FilterableResource` points at, without knowing which specific one. Each implementer specializes the shared interface's `@template TModel` via `@extends FilterablePaginator<ItsModel>` -- a bare unparameterized return type didn't work, since Laravel's `LengthAwarePaginator` isn't `@template-covariant`, so every implementer's more specific `LengthAwarePaginator<int, ItsModel>` return type would otherwise be incompatible with the shared interface's. No behavior change -- verified with the full `Reseller|User|Course|QuizAttempt|Activity|Architecture` test filter (330 tests) plus a full `phpstan analyse app`.
  - [x] Export request/job/download mechanism (core, one resource type end-to-end). `exports` table + `App\Models\Export` (scoped by `user_id` for ownership, not `TenantScoped` -- `reseller_id` here is operational metadata telling the queued job which `TenantContext` to set before calling a tenant-scoped repository, not an ownership column, so `TenantScoped` would be actively wrong and would hide a platform-wide export, whose `reseller_id` is null, from its own owner; added to `TenancyAuditCommand::EXEMPT` with that reasoning). `App\Services\Exporting\ExportResourceRegistry` maps `FilterableResource` to the repository + `JsonResource` its index page already uses -- `GenerateExportJob` reuses that same `JsonResource::toArray()` for CSV row-shaping rather than a second, parallel set of column definitions. `RequestExport` action creates the `Export` row and dispatches the job; `ExportController` (`store`/`index`/`download`) mirrors each index's own authorization check per resource type. Download links are `URL::temporarySignedRoute()`, generated fresh on every `ExportResource` serialization (never persisted) and independently re-checked against `Export::isDownloadable()`/ownership server-side -- proven by dedicated tests for wrong-owner, unsigned/tampered, and expired-link cases. Hit real generic-variance friction getting `ExportResourceRegistry` to work across all 7 differently-specialized repositories (Laravel's `LengthAwarePaginator` isn't `@template-covariant`, so no shared return type could describe "some `LengthAwarePaginator<int, X>`" -- worked around by resolving+paginating+extracting `items()` all inside one `match()`, since plain arrays don't carry the same generic-invariance restriction). Wired end-to-end into the resellers index (`Export as CSV` button, `useExportRequest` composable) as proof; the next sub-task adds it to the other six.
  - [x] Wire remaining filterable indexes into the export mechanism. Mechanical: added the `Export as CSV` button (`useExportRequest` composable, matching `FilterableResource`'s own values) to users, klanten, courses, assignments, attempts, and activity -- the same pattern proven on resellers in the sub-task above. **Fixed a second occurrence of the dead-`<slot name="nav">` bug** (see the platform-dashboard task's note) in `AppLayout.vue`, discovered while deciding where the shared "My exports" page should live: it's reachable from both platform and reseller admin areas, so it uses `AppLayout` rather than forcing either `AdminLayout` or `ResellerAdminLayout`'s role-specific sidebar -- which meant actually exercising `AppLayout`'s own nav slot for the first time. Replaced with real links (`Dashboard`, `My exports`) the same way; added a `My exports` link to `AdminLayout`/`ResellerAdminLayout` too.
  - [ ] XLSX format. Deliberately not built alongside CSV: it would need a new Composer dependency (`phpoffice/phpspreadsheet` or similar) purely for spreadsheet-specific features (multiple sheets, styling) that nothing here actually needs -- every consumer of an export opens it in Excel/Sheets either way, and CSV already does that natively. Left as its own explicit follow-up rather than silently claiming this task complete against its literal "CSV/XLSX" wording.
- [x] Scheduled reports emailed to klant admins. `App\Mail\KlantProgressReport` (reseller-branded via the existing `HasResellerBranding` trait, same eager-constructor-resolution pattern as `UserInvited` since queued Mailables have no ambient tenant on a worker) -- deliberately plain, not routed through `MailTemplateRenderer`'s override system: that system is built for short placeholder-substitution transactional emails, and this carries a per-cursist data table, a poor fit for that model, so resellers get branding but not content customisation here. Reuses `KlantDashboardService::forKlant()` (already built for the Klant Dashboard task) as the data source rather than a second aggregation. `App\Actions\Reporting\SendKlantProgressReport` resolves recipients strictly as users with `hasRole('klant-admin')` (not `klant-manager` -- this task's own wording says "klant admins"), setting `PermissionRegistrar::setPermissionsTeamId($klant->reseller_id)` first since a scheduled command has no per-request tenant resolution (same pattern as `UserPolicy::impersonate()`). New `ResellerKlantRepository::all(): LazyCollection` iterates every klant across every reseller, deliberately bypassing `TenantScoped` with a documented comment -- a platform-context scheduled job, not a tenant-facing read. `App\Console\Commands\SendKlantProgressReportsCommand` (`reports:klant-progress`) is the first real consumer of Laravel's scheduler in this codebase -- registered in `routes/console.php` via `Schedule::command(...)->weeklyOn(1, '06:00')`; the Ploi cron entry pointing at `schedule:run` (Phase 1) had nothing to actually run until now.
- [x] Platform health: queue depth, failed jobs, storage, error rate. **Process note**: done out of order ahead of "Scheduled reports" above it (CLAUDE.md §0 rule 1 is strict top-to-bottom) -- caught only after the fact; there was no dependency between them either way, so leaving this checked rather than discarding correct, tested work, but flagging the slip rather than pretending it didn't happen. New `App\Models\FailedJob` (maps Laravel's own `failed_jobs` table -- platform-wide by nature, outside `TenancyAuditCommand`'s scope entirely since it has no `reseller_id` column to flag) + `FailedJobRepository`. Queue depth comes from Horizon's own `Laravel\Horizon\Contracts\WorkloadRepository::get()` (per-queue pending length/wait time/worker count), not a hand-rolled Redis query. "Error rate" (this task's own wording) is approximated as failed jobs in the last 24h -- Sentry is currently write-only in this codebase (reports errors, no API client configured to query counts back out), so this is the closest real, locally-available signal rather than a guess at an integration that doesn't exist; documented as a deliberate interpretation, not silently assumed. `App\Services\Reporting\PlatformHealthService` composes all three (queue workload, failed jobs, `MediaRepository::totalBytesAcrossPlatform()` reused from the platform dashboard) into one snapshot. `PlatformHealthController`, `GET /admin/platform/health`, `Admin/Platform/Health/Index.vue`.
- [x] vue-i18n is not actually installed/configured anywhere yet, despite CLAUDE.md §6 requiring it for every user-facing string. Every existing page (`Klanten/Index.vue`, `Dashboard.vue`, etc.) hardcodes strings in one language or the other. Discovered while building the Phase 7 admin dashboard -- out of scope to retrofit inline (it's a codebase-wide setup + a pass over every existing page), so tracked here per CLAUDE.md §0 rule 6 rather than done ad hoc. Blocked Phase 8 under CLAUDE.md §0 rule 2, so closed out here rather than left deferred. Split into sub-tasks (CLAUDE.md §0 rule 4) -- 80 `.vue` files existed at the time, too large for one commit; each sub-task below is its own commit, and `npm run types`/`npm run lint`/`npm run build` were run clean after every one:
  - [x] Infrastructure: installed `vue-i18n`. `resources/js/i18n.ts` exports `createAppI18n(locale)` (Composition API mode, `legacy: false`); `resources/js/locales/{en,nl}.json` hold nested message catalogues keyed by area (`layouts.admin.*` etc.). Locale is decided server-side, not client-side -- `HandleInertiaRequests::share()` now sends `'locale' => app()->getLocale()`, read once at boot in `app.ts` from the initial page props and passed into `createAppI18n()`; there's no runtime language switcher (not requested, would be speculative), so no re-sync after boot. Also changed `config('app.locale')`'s default from Laravel's skeleton `'en'` to `'nl'` (and `.env.example`'s `APP_LOCALE`) -- this product's own domain vocabulary (cursist, klant, resellerklant, voorlees) and its existing hardcoded UI strings are predominantly Dutch, so `'en'` was never an honest default; still fully overridable per-environment. `resources/js/types/inertia.d.ts` gained `locale: string` on `sharedPageProps`. Migrated the 5 layouts (`AdminLayout`, `AppLayout`, `GuestLayout` -- no translatable strings, `KlantAdminLayout`, `ResellerAdminLayout`) as the first real consumers, proving the pattern end-to-end (`npm run types`, `npm run lint`, `npm run build` all clean).
  - [x] Migrate Auth pages (Login, ForgotPassword, ResetPassword, TwoFactorChallenge, VerifyEmail, AcceptInvite) -- new `auth.*` message namespace in both locale files, one sub-key per page. `AcceptInvite`'s title/description use vue-i18n named interpolation (`t('auth.acceptInvite.title', { name })`) for the invitee's name/email rather than string concatenation.
  - [x] Migrate Admin/Platform pages (Dashboard, Resellers/Index, Users/Index, Users/Show, Activity/Index, Health/Index). Introduced a `common.*` namespace (`exportCsv`, `pagination`, `noResultsFiltered`) shared across every filterable index page rather than repeating near-identical strings per resource -- the empty-state message is now a single generic "No results match these filters." instead of a per-resource variant ("No resellers match...", "No users match...").
  - [x] Migrate Admin/Reseller + Admin/Klant pages (Dashboard, Courses/Index, Assignments/Index, Attempts/Index, Klant/Dashboard) + Klanten/Index, Invites/Index, Admin/Exports/Index. `Klanten/Index.vue` and `Invites/Index.vue` were the two pages that most visibly mixed Dutch and English strings before this task (the motivating example named in this task's own original wording) -- now fully driven by the `klanten.*`/`invites.*` namespaces. "Klant"/"Cursist" translate to "Client"/"Student" in the English catalogue but stay as the Dutch domain terms in `nl.json`.
  - [x] Migrate Settings pages (Theme, TwoFactorAuthentication, EmailTemplates/Index, EmailTemplates/Edit) + misc pages (Dashboard, Welcome, Errors/Error, Certificates/Verify). `Errors/Error.vue`'s status-to-message lookup switched from a plain object literal to `te()`/`t()` against an `errors.<status>` key (`te` = "translation exists") so an unmapped status code still falls back to `errors.unknown` the same way the old `??` fallback did.
  - [x] Migrate shared components (AppFooter, ImpersonationBanner, LocaleField, TranslationCompletenessIndicator, VoorleesControls). `ImpersonationBanner`'s "X is impersonating Y" sentence mixes translatable copy with `<strong>` markup around each name -- a plain `t()` call would either lose the bold styling or require raw HTML interpolation, so it uses vue-i18n's `<i18n-t>` component with named slots (`#impersonator`/`#target`) instead, the standard idiom for markup-preserving interpolation.
  - [x] Migrate Question editor components (14 files, `Components/Questions/Editor/*`). Shared `questions.common.remove` key for the "Remove" button repeated in every list-editing type. `FillInBlankQuestion`/`DropdownInTextQuestion`'s "Template (use {{id}} for each blank)" label hit a real parsing trap: writing the interpolated message inline in the template put the literal `{{id}}` placeholder text inside the template's own `{{ }}` mustache delimiters, which Vue's compiler tried to parse as nested interpolation (`Unexpected end of expression`) -- fixed by moving the `t()` call into a `computed()` in `<script setup>` instead, so the template only ever sees a plain `{{ templateLabel }}` reference.
  - [x] Migrate Question player components (14 files, `Components/Questions/Player/*`). Only the UI chrome (labels, aria-labels, empty-state text) was translated -- option/pair/item text itself is cursist-authored *course content*, not application UI, so it stays exactly as stored (that distinction already exists in this codebase: content translation is `spatie/laravel-translatable`'s job per CLAUDE.md §1, vue-i18n is only for interface strings). `MultipleChoiceQuestion`, `MultipleResponseQuestion`, and `LikertQuestion` players had no translatable chrome at all -- left untouched.
  - [x] Migrate Block editor + player components (14 files, `Components/Blocks/{Editor,Player}/*`). All 7 editor components had real chrome to translate (labels, help text, the callout style dropdown's variant names). Only 2 of the 7 player components did (`EmbedBlock`/`VideoEmbedBlock`'s iframe `title` attribute) -- the rest (`Callout`, `Divider`, `FileDownload`, `Image`, `RichText`) render nothing but block content itself, which is course-authored, not application UI. This was the last sub-task -- the vue-i18n retrofit (CLAUDE.md §6) is now complete across all 80 `.vue` files that existed when this task was opened.

---

## Phase 8 — Notifications, billing, GDPR

### Notifications

- [x] Notification catalogue (welcome, invite, assignment, deadline, overdue, completion, certificate, password, billing, admin alerts). `App\Enums\NotificationType` -- ten cases matching this task's own list, each with `label()`. Deliberately just the enum, not yet the `App\Notifications` classes themselves: a Laravel `Notification` class bundles its channel selection (`via()`) and per-channel rendering (`toMail()`/`toDatabase()`) in one place, so there's no way to build "the catalogue" and "the channels" as two genuinely separate, individually-complete pieces of code the way TODO.md's next line implies -- splitting them would mean committing dead/inert classes now. What *can* stand alone, and what every later task in this section (channels, per-user preferences, digesting, reseller mail-template overrides) needs to already exist, is the fixed set of category identifiers itself -- so that's what this task delivers. Confirmed via research: `User` already has `Notifiable` (unused so far), password reset already runs through Laravel's own default `ResetPassword` notification (no custom code), and `AssignmentDeadlineService` (built in Phase 5) already has a docblock note that its mailable/dispatch is explicitly "Phase 8's job" -- exactly the next task.
- [x] Email + in-app database channel for each. Split into sub-tasks (CLAUDE.md §0 rule 4) -- 10 catalogue entries, several needing new trigger points that don't exist yet:
  - [x] Infrastructure: `notifications` table (Laravel's standard schema -- no `reseller_id`, a notification's tenant follows its notifiable owner's, same as every other owner-follows-parent table). `App\Notifications\WelcomeNotification` (`via()` → `['mail','database']`) is the reference implementation, fired from `AcceptInvite` after its transaction commits (not from inside it, so a queued send can never race a not-yet-committed `User` row). `toMail()` returns the existing `App\Mail\Welcome` mailable (new, reuses `HasResellerBranding`, same shape as `UserInvited`/`KlantProgressReport`) -- hit a real bug here: when `toMail()` returns a `Mailable` instance rather than a `MailMessage`, Laravel's mail channel calls `$message->send()` directly without ever addressing it, so the mailable itself must carry `->to($notifiable->email)` or delivery fails with "An email must have a To, Cc, or Bcc header" (caught by `InviteAcceptTest`, not a unit test, since only the full HTTP-level test exercises delivery through the notification's mail channel rather than constructing the mailable directly). Also added `App\Notifications` to `tests/Architecture/LayeringTest.php`'s model-access whitelist, same reasoning already given there for `App\Mail`: a notification class is typed over `via($notifiable)`/`toMail($notifiable)`/`toDatabase($notifiable)`, so it necessarily accepts an already-loaded model.
  - [x] Assignment notification -- `App\Notifications\CourseAssignedNotification` + `App\Mail\CourseAssigned`, fired from `AssignCourseToCursist` after its transaction commits. `BulkAssignCourseToCursists`/`AssignCourseToGroup` both funnel through that one action, so they get the notification for free with no separate wiring. Queried `$assignment->user()->first()` explicitly rather than `$assignment->user` -- the model is freshly created with no relations loaded, and lazy loading is disabled outside production.
  - [x] Deadline + Overdue notifications. New `assignment_reminders` table (reseller-scoped, `TenantScoped` like its parent `CourseAssignment`) is pure dedup bookkeeping -- "has a deadline reminder at this offset, or the one overdue notice, already gone out for this assignment" -- unique on `(course_assignment_id, type, days_before)`. `App\Actions\Notifications\SendAssignmentDeadlineReminder` evaluates one assignment against "today": skips entirely if the course is already complete (via the existing `CourseCompletionChecker`, even past the deadline), otherwise sends at most one `AssignmentOverdueNotification` (once, ever) or one `AssignmentDeadlineNotification` per configured offset that's due and not yet sent -- deliberately does not modify `AssignmentDeadlineService` itself (Phase 5), which stays pure calculation; this Action owns exactly the "mailable, dispatch, sent-tracking" split its docblock called out as Phase 8's job. New `CourseAssignmentRepository::dueForDeadlineEvaluation()` (platform-wide, bypasses tenant scope, same reasoning as `ResellerKlantRepository::all()`) feeds a new daily `App\Console\Commands\SendAssignmentDeadlineRemindersCommand` (`reports:assignment-deadlines`, one Action call per assignment -- same Action/Command split as `SendKlantProgressReport`/`SendKlantProgressReportsCommand`), registered at `07:00` daily in `routes/console.php`.
  - [x] Completion notification -- new `course_assignments.completed_at` (nullable, stamped once), a pure one-time-fire marker: `CourseCompletionChecker` stays the only source of truth for "is this complete *right now*" (still computed fresh everywhere else, e.g. `IssueCertificate`), this column only remembers "have we already notified for this one". Wired into `MarkBlockCompleted` (Phase 5): after recording block progress, if `completed_at` is still null and the course *is* now complete, stamp it and fire `CourseCompletedNotification`. **Real, documented gap, not silently papered over**: `MarkBlockCompleted` is the only write path today that can flip completion under the default `all_lessons` rule -- the `pass_exam`/`minimum_score` rules key off `QuizAttempt::submitted_at`, but grepping the whole codebase turned up no Action anywhere that actually submits a quiz attempt (`submitted_at` is only ever set via factory states in tests). Courses using those two rules won't get a Completion notification until that submission Action exists -- out of scope here, since building it would be a full quiz-taking feature, not a notification-catalogue task.
  - [x] Certificate notification -- `App\Notifications\CertificateIssuedNotification` + `App\Mail\CertificateIssued`, fired from `IssueCertificate` right after the `Certificate` row is created (no transaction wraps that action, unlike Assignment/Welcome, so no commit-ordering concern). Links to the existing public `certificates.verify` route rather than inventing a download mechanism -- no authenticated "download my certificate" page exists yet, and building one is out of scope for a notification task. Caught a second real tenant-scoping bug while writing this: `Certificate` itself isn't `TenantScoped` (no `reseller_id`, ownership follows its parent per its own docblock), but its `courseAssignment()` relation points at `CourseAssignment`, which *is* -- so resolving that relation from a queued Mailable with no ambient tenant silently returned null and the constructor always threw. Fixed with `->withoutTenantScope()` on that one relation query, documented inline; caught by `IssueCertificateTest`'s three non-`Notification::fake()`d tests, which exercise the real `toMail()` path end to end.
  - [x] Admin alert notification. `App\Notifications\AdminAlertNotification` is the one catalogue entry with no reseller in the picture at all (recipients are platform super-admins), so it deliberately uses Notification's own `MailMessage` builder instead of an `App\Mail\*` class + `HasResellerBranding` -- there's no reseller to brand as the sender. New `App\Actions\Notifications\CheckPlatformHealthAndAlert` reuses Phase 7's `PlatformHealthService::snapshot()` (kept pure -- this Action is the "is that bad enough to page someone, and send" layer on top of it) and alerts every `UserRepository::superAdmins()` (new method, `platform_role = super-admin`) when `failedJobCountLast24Hours` exceeds a threshold (10). Debounced with a plain cache TTL (24h), not a table -- this is a "don't spam every hourly run while the spike persists" concern, not a record that needs to survive a cache flush. New hourly `App\Console\Commands\CheckPlatformHealthCommand` (`health:check`), registered in `routes/console.php`.
  - [x] Password -- already fully satisfied: `User` has no `sendPasswordResetNotification()` override, so Laravel's own built-in `Illuminate\Auth\Notifications\ResetPassword` already fires via the `mail` channel on every reset request. No database channel makes sense for a reset link (not something to browse later in a notification centre). Documented, no code change.
  - [x] Invite -- already fully satisfied by the existing `App\Mail\UserInvited` flow (Phase 1/7). No database channel applies: the recipient has no `User` row (and therefore no notifications relation) until they accept. Documented, no code change.
  - [x] Billing -- deliberately not built here. No billing domain event exists yet anywhere in the codebase (the entire Billing section below is still unchecked) -- there is nothing to trigger on. Revisit as part of the Billing section itself once `PaymentGateway`/webhooks/billable-event-on-assignment exist, rather than faking a trigger now. Tracked so this isn't silently forgotten: re-open this checkbox when the Billing section below is built.
- [x] In-app notification centre with read state. New `App\Contracts\Repositories\NotificationRepository` reads over Laravel's own `Illuminate\Notifications\DatabaseNotification` (not an `App\Models` class -- `Notifiable` already owns the relationship, there's nothing app-specific to model), scoped by `int $userId` rather than a `User` model, same established convention as `ExportRepository` so controllers never need to import `App\Models\User`. `MarkNotificationRead`/`MarkAllNotificationsRead` Actions. `GET /notifications`, `POST /notifications/{id}/read`, `POST /notifications/read-all` -- `NotificationController` mirrors `ExportController`'s own-record 404 check (a notification not owned by the requesting user 404s, not just gets excluded from the index). `Notifications/Index.vue` on `AppLayout` (reachable from every area, same reasoning as My exports). Retrofitted a consistent `message` field into every notification type's `toDatabase()` payload as a prerequisite -- see the standalone consistency commit just before this one -- so the centre has one generic way to render a line for any type rather than needing type-specific frontend logic. Added a shared `notificationsUnreadCount` Inertia prop (`HandleInertiaRequests::share()`) and a badge + nav link to all 5 layouts.
- [x] Per-user notification preferences, per type, per channel. Opt-out model: new `notification_preferences` table (`user_id`, `type`, `channel`, `enabled`, unique per combination) only ever holds an explicit override -- absence means enabled, matching `NotificationPreferenceRepository::isEnabled()`'s `?? true`. Not `TenantScoped` (personal, same as `SavedFilter`). New `App\Notifications\Concerns\RespectsPreferences` trait, applied to the 6 preference-eligible notification classes' `via()` (Welcome, Assignment, Deadline, Overdue, Completion, Certificate) -- deliberately **not** applied to `AdminAlertNotification` (a platform health alert shouldn't be silenceable by an individual super-admin) or Password/Invite (Laravel's own built-in notification and a not-yet-a-`User` recipient, neither fits a `User`-keyed lookup). `App\Services\Notifications\NotificationPreferenceGridService` merges the fixed 6-type catalogue with a user's sparse overrides into the full type × channel grid the settings page renders -- a Service, not a Repository method (pure calculation, not a query) or an `Http\Resources` class (no single model/collection to shape). `GET/PUT /settings/notifications`, `Settings/NotificationPreferences.vue` -- not linked from any nav yet, matching the existing Theme/2FA/Email-templates settings pages, which are also direct-URL-only today.
- [x] Digest option (daily/weekly) instead of individual emails. One account-wide setting, not per-type like the channel preferences above -- a digest cadence is inherently a single "how often do you want email" knob. New `users.notification_digest_frequency` (`App\Enums\DigestFrequency`: Immediate/Daily/Weekly, default Immediate) + `users.notification_digest_sent_at` (nullable cutoff timestamp). `RespectsPreferences::filterByPreference()` extended: after the existing per-type-channel filtering, if the user's digest frequency isn't Immediate, `mail` is stripped from the remaining channels -- the `database` write (if still enabled) goes through untouched, since that's what the digest itself later reads from. No new "pending digest item" table: the digest is just `notifications` rows with `created_at > notification_digest_sent_at`, via new `NotificationRepository::createdSince()`. `App\Actions\Notifications\SendNotificationDigest` is per-user (`__invoke(User $user): bool`, false when there's nothing new -- a no-op, not an error), builds the one messages list off each notification's already-standardized `data['message']` field (see the earlier `message`-field consistency task), sends `App\Mail\NotificationDigest`, and only then stamps the new cutoff. `NotificationDigest` is deliberately the one Mailable in the codebase that does *not* throw for a reseller-less recipient (`HasResellerBranding` degrades to unbranded) -- platform staff can be digested too. Two scheduled commands share the Action: `notifications:digest-daily` (08:00 daily) and `notifications:digest-weekly` (Monday 08:00), both iterating a new `UserRepository::withDigestFrequency()` (platform-wide read, same bypass-tenant-scope reasoning as `ResellerKlantRepository::all()`). Settings UI: `PUT /settings/notifications/digest-frequency` (`App\Actions\Notifications\UpdateNotificationDigestFrequency`) + a `<select>` added above the existing type×channel grid on `Settings/NotificationPreferences.vue`. Two real bugs caught while testing: (1) `NotificationRepository::createdSince()`'s `$since` parameter was typed `?Illuminate\Support\Carbon`, but `notification_digest_sent_at` casts to `immutable_datetime` (`CarbonImmutable`) -- retyped to `?Carbon\CarbonInterface`, which both implement; (2) `NotificationDigest` implements `ShouldQueue`, so Laravel's `Mail::fake()` records it under "queued", not "sent" -- the Action test asserts `Mail::assertQueued()`/`assertNothingOutgoing()`, not `assertSent()`/`assertNothingSent()`.
- [x] Reseller-branded email templates applied to all of the above. Two distinct existing concepts, both extended to cover every notification-catalogue mailable (not just `UserInvited`, the only one that had either before): (1) `HasResellerBranding`'s sender-name/reply-to envelope customization -- already applied everywhere; (2) actual visual branding (accent color, logo, footer) in the mail *body*, which nothing used yet -- this task built that half. New `resources/views/vendor/mail/html/themes/reseller.blade.php`: a Blade copy of Laravel's own default markdown theme CSS with the accent-color declarations (link, header link, button, panel border) parameterized as `{{ $primaryColor ?? '#0d6efd' }}`. Must be `.blade.php`, not `.css` -- `Illuminate\Mail\Markdown::render()` only evaluates the theme file through Blade when given that extension; `.css` is served raw via the `file` engine and can't read a variable. New `HasResellerBranding::brandingData()` returns the one array (`resellerName`, `primaryColor`, `logoUrl`, `footerText`, `supportEmail`, `termsUrl`, `privacyUrl`) every branded Mailable spreads into its `Content(with: ...)`; each sets `$this->theme = 'reseller'` in its constructor as an *untyped* assignment (not a re-declared typed property -- Larastan's `property.extraNativeType` flags giving a native type to an override of `Mailable::$theme`, which is itself untyped). `logoUrl` goes through the existing public, unauthenticated `branding.logo` route (`ResellerBrandingController`), not the raw private-disk path -- an email client fetches images over plain HTTP with no session, unlike the certificate PDF's dompdf renderer which reads the disk path directly. New shared partials `resources/views/emails/partials/branded-{header,footer}.blade.php`, `@include`d from inside `@slot('header')`/`@slot('footer')` blocks added to all 7 affected views (`welcome`, `assignments/{assigned,deadline-approaching,overdue,completed}`, `certificates/issued`, `invites/invited`) -- deliberately NOT achieved by overriding Laravel's `mail::message`/`header`/`footer` components globally, since `@component()`'s data scope is the component's own explicit data/slots, not the calling view's variables; an `@include`d partial, by contrast, compiles inline in the parent's own PHP scope and so sees `$resellerName`/`$logoUrl` for free. `NotificationDigest` (no fixed body, aggregates arbitrary messages) and `AdminAlertNotification`/`KlantProgressReport` (no single reseller, or not part of this catalogue) deliberately excluded, consistent with their earlier documented exclusions from the subject/body override system. **Caught and fixed a real, pre-existing bug while adding render-based tests for this** (none of the prior mail/notification tests ever called `->render()`, only inspected `envelope()`/`content()`): `User::notification_digest_frequency` has a DB column default of `'immediate'`, but a freshly-instantiated (not `->fresh()`d) `User` -- e.g. right after `User::factory()->create()`, or `AcceptInvite`'s own newly created user -- never had that default reflected in memory (Eloquent doesn't re-fetch DB-side defaults after an insert), so `RespectsPreferences` read it as `null`, treated that as "not Immediate", and silently stripped the `mail` channel from every notification for every just-created user. Fixed at the root cause with a `protected $attributes = ['notification_digest_frequency' => 'immediate']` default on the `User` model itself (mirrors the DB default so even a pre-save, in-memory instance already carries the right value) rather than a defensive null-check in `RespectsPreferences`.
- [x] Bounce/complaint handling, suppression list. No mail provider was pinned anywhere in the codebase yet (`.env.example`'s `MAIL_MAILER=log` is just the local default) -- picked Mailgun as the reference webhook integration over Postmark/SES: its HMAC signature scheme is simple and self-contained, unlike SES's SNS subscription-confirmation handshake. New `suppressed_emails` table -- deliberately platform-wide, not `TenantScoped`: a hard bounce or spam complaint is a property of the recipient mailbox, not of whichever reseller's course happened to email it, so it must block sending regardless of which tenant tries next. `App\Contracts\Mail\MailSuppressionWebhookParser` (mandatory interface per CLAUDE.md §3a -- an external service integration that must be fakeable/swappable) + `App\Services\Mail\MailgunWebhookParser` verifies the timestamp+token HMAC and maps Mailgun's event taxonomy (`failed`+`severity: permanent` -> hard bounce, `complained` -> complaint; `failed`+`severity: temporary`, `delivered`, `opened`, etc. -> not suppression-relevant, returns null) onto a normalized `MailSuppressionEventData` DTO -- same "normalized, idempotent, signature-verified" webhook shape the Billing section's Mollie webhook will reuse. `App\Actions\Mail\RecordMailSuppression` upserts keyed on the unique `email` column, so a provider's own at-least-once retry delivery doesn't create duplicates. `MailWebhookController::mailgun()` (public, unauthenticated, CSRF-exempted in `bootstrap/app.php`, rate-limited per IP) verifies then records; 401s on a bad signature, 200s (no-op) on a signature-valid but non-suppression event. Enforcement is one global hook, not a per-Mailable check: `App\Listeners\Mail\PreventSendingToSuppressedAddresses` listens for `Illuminate\Mail\Events\MessageSending` and returns `false` to cancel the send outright when any To/Cc/Bcc address is suppressed -- deliberately NOT queued, since it must run synchronously in the same process as the send it's allowed to cancel. Registered in `AppServiceProvider::boot()`, matching this app's existing style (no `EventServiceProvider` exists yet, following the same "register in AppServiceProvider" pattern already used for `Gate::before`). No admin UI to browse the list yet -- same "backend only, no consuming page yet" precedent already used for `SavedFilter`.

### Billing

- [x] `PaymentGateway` interface + `MollieGateway` implementation. The interface itself was already locked in during Phase 0 (`createPayment(int $amountCents, string $description, string $redirectUrl): array`, `getPaymentStatus(string $paymentId): string`), with `tests/Fakes/FakePaymentGateway` already bound globally in `tests/TestCase.php` for every test -- this task only had to build the real implementation. `App\Services\Billing\MollieGateway` talks to Mollie's REST API directly via Laravel's own `Illuminate\Http\Client\Factory`, the same shape as the existing `App\Services\Ploi\HttpPloiClient`, rather than pulling in the `mollie/mollie-api-php` SDK -- Mollie's API is a plain JSON REST API underneath, so a real dependency wasn't needed, and this keeps it trivially testable with `Http::fake()` (mirroring `HttpPloiClientTest`) instead of having to fake a third-party SDK client. `createPayment()` posts amount (formatted as a `"12.34"` string per Mollie's API, converted from cents), description, and redirect URL, and returns `id`/`checkoutUrl`/`status` (defensively `is_string()`-checked against the decoded JSON response, matching the established defensive-parsing convention from `MailgunWebhookParser`, rather than trusting the shape). `getPaymentStatus()` is a plain GET. Bound in `AppServiceProvider` (config-based API key via `services.mollie.key` / `MOLLIE_KEY`, already reserved in `.env.example` since Phase 0), same closure-construction pattern as the `PloiClient` binding right above it.
- [x] Pricing: `max(15% × reseller_price, €3.00)` for reseller courses; fixed platform price for catalog courses. Already fully built (an earlier Phase 5/7-era commit, before this Billing section was reached top-to-bottom) as `App\Services\Billing\AssignmentPricingService::priceFor()`, wired into `AssignCourseToCursist` to snapshot `CourseAssignment.price_cents` at assignment time. Only the TODO.md checkbox was outstanding -- checking it off now that the Billing section is actually being worked, rather than leaving it looking unbuilt.
- [x] Platform admin manual price-floor override per reseller course. Also already built as part of the same `AssignmentPricingService` (`Course.price_floor_override_cents`, applied as a `max()` after either pricing branch, never auto-detected per §11). Checkbox-only catch-up, same as the task above.
- [ ] Reordered below into the real dependency chain (CLAUDE.md §0 rule 4): there is no `Invoice` model yet anywhere in the codebase, and "normalized webhook handler" / "billable event" / "invoice generation" / "dunning" / "reseller billing dashboard" all need one to exist and mean the same thing before they can be built individually. Sub-tasks, in build order:
  - [x] Invoice + InvoiceLine domain model (immutable once issued, one line per billed `CourseAssignment`). `App\Enums\InvoiceStatus` (Draft/Issued/Paid/Overdue/Void, plus `isOpenForNewLines()` -- only a Draft may still receive a new line). `Invoice` is `TenantScoped` (reseller-owned, gets its isolation test like every other tenant-scoped model); `InvoiceLine` is deliberately not -- ownership follows its parent `Invoice`, same "owner-follows-parent" convention already established by `Certificate` following `CourseAssignment`. `InvoiceLine.description` is a snapshot string, not a live join to `courses.title`: the course can be renamed or deleted long after a line is issued, and an issued invoice's own line text must never silently change retroactively. New `App\Contracts\Repositories\InvoiceRepository::currentDraftForReseller()` (read-only, per CLAUDE.md §3a) is the one method this task needed -- "the still-open invoice a billable event may attach a line to" -- deliberately minimal rather than building the whole repository surface speculatively; the next task (billable event) is what will actually create/update invoices via an Action. `total_cents`/`amount_cents` both use the existing `MoneyCast`. No VAT column yet -- kept out until the VAT task actually needs it, rather than adding a column no code reads yet.
  - [x] Billable event on course assignment (attaches a line to the reseller's current period, using the pricing already built above). New `App\Actions\Billing\RecordBillableAssignment`, called from inside `AssignCourseToCursist`'s own transaction (a nested transaction via SAVEPOINT, since both belong to the same billable event -- CLAUDE.md §11 literally defines "assigning one course to one cursist" as the billable event, not something deferred): finds the reseller's current-period `Invoice` via `InvoiceRepository::currentDraftForReseller()`, creates one if none exists yet (period = current calendar month), attaches an `InvoiceLine` (description snapshotted from the course title, amount from the assignment's already-priced `price_cents`), and flips the assignment's `billing_state` from Pending to **Billed immediately** -- not deferred until some later invoice-issuing step. This is a deliberate reading of §11: the 14-day free-revocation rule is a later *reversal* (a credit/waiver) applied on top of an assignment that was already billed here, the same way most real invoicing works (bill now, credit back if the grace-period condition is met), not a reason to delay billing speculatively. `BulkAssignCourseToCursists`/`AssignCourseToGroup` get this for free, same as the notification -- both already funnel through the one Action. **Real bug caught before it shipped**: the natural way to add to the invoice's running total, `$invoice->increment('total_cents', $amountCents)`, would have crashed -- Eloquent's `increment()` evaluates `$this->{$column} + $amount` when the cast class doesn't implement its own `increment()`/`decrement()` methods (`MoneyCast` doesn't), and `total_cents`'s getter returns a `Money` object, so `Money + int` throws a `TypeError`. Fixed by reading the current cents out, adding in plain ints, and reassigning a new `Money::fromCents(...)`. Updated `AssignCourseToCursistTest`'s existing "pending" expectation to "billed" (the old assertion predates this task and is no longer the intended behavior) and added two new tests covering the invoice-line side directly, plus a dedicated `RecordBillableAssignmentTest` (new invoice created, existing draft reused and totals accumulated across two assignments, and an already-issued invoice is left alone -- a fresh draft is opened instead).
  - [x] Invoice generation (immutable once issued): moved ahead of the webhook handler below -- a webhook that reacts to Mollie payment status is meaningless until something actually creates a Mollie payment and stamps an invoice with its id. Credit notes for corrections are a separate later sub-task, once VAT (right after this) settles what a correction actually needs to adjust. New `App\Actions\Billing\IssueInvoice` (Draft -> Issued exactly once, refuses a non-draft or a EUR 0.00 invoice -- an empty period is never charged) calls `PaymentGateway::createPayment()` for the invoice's already-accumulated total and stamps `mollie_payment_id`/`issued_at`/`due_at` (14 days, matching the revocation window's own grace period). New `InvoiceRepository::draftsReadyToIssue()` (platform-wide, `withoutTenantScope()`, same reasoning as `CourseAssignmentRepository::dueForDeadlineEvaluation()`) feeds a new daily `App\Console\Commands\IssueDueInvoicesCommand` (`billing:issue-invoices`, 02:00) -- daily rather than a fixed "1st of the month" cron entry, since the repository query itself (`period_end < now()` and `total_cents > 0`) is what actually determines readiness; running daily just catches a just-ended period promptly. **Real tenant-scoping bug caught before it shipped, same class of bug as `SendAssignmentDeadlineReminder`'s and `GenerateExportJob`'s documented fixes**: `Invoice` is `TenantScoped`, and a batch command issuing invoices for many resellers in one run has no single ambient tenant -- without setting one, `$invoice->save()` would have silently updated zero rows (`TenantScope` fails closed to `1=0` with nothing resolved), while the in-memory model would still *look* correctly updated, masking the failure. Fixed by injecting `TenantContext` into `IssueInvoice` and setting it to the invoice's own reseller before saving, mirroring the established fix. Caught a second, smaller bug via Larastan: direct property assignment (`$invoice->issued_at = now()`) doesn't accept `Illuminate\Support\Carbon` against a property typed `CarbonImmutable` by its cast, unlike the array-based `forceFill()` used elsewhere in the codebase (which isn't statically checked per-key the same way) -- fixed with `->toImmutable()`. No reseller-facing invoice page exists yet, so the Mollie `redirectUrl` points at the reseller admin dashboard as a placeholder, to be replaced once the "reseller billing dashboard" sub-task builds a real one.
  - [x] Normalized Mollie webhook handler (idempotent, replay-safe -- Mollie's own recommended pattern: the webhook body carries only a payment id, never trusted directly, always re-verified via an authenticated `PaymentGateway::getPaymentStatus()` call back to Mollie). No separate parser/DTO layer was needed the way the Mailgun suppression webhook has one: `PaymentGateway` itself already *is* the normalized abstraction here, and Mollie's webhook body is trivial (just `id`) -- there's no provider-specific shape left to normalize. New `App\Actions\Billing\RecordInvoicePaymentStatus`: looks up the `Invoice` by `mollie_payment_id` (no-op, not an error, if none matches -- a stale or unrelated webhook call), then calls `PaymentGateway::getPaymentStatus()` and maps `paid` -> `InvoiceStatus::Paid` (stamps `paid_at`) or `expired`/`canceled`/`failed` -> `InvoiceStatus::Overdue` (retry/suspension logic itself is the separate "Dunning" sub-task below); any other status (`open`, `pending`, `authorized`) leaves the invoice untouched. Idempotent by an explicit early return when the invoice is already in its target-equivalent state, not just by database upsert semantics. `App\Http\Controllers\MollieWebhookController` (public, unauthenticated, CSRF-exempted, rate-limited per IP, same shape as `MailWebhookController`) validates only that `id` is a non-empty string before delegating -- 422 on a missing id, 200 otherwise (even for an unrecognized payment, since retrying wouldn't help). Reused the same "set ambient `TenantContext` to the invoice's reseller before writing" fix this Billing section already needed twice (`RecordBillableAssignment`, `IssueInvoice`) -- webhook calls have no ambient tenant at all.
  - [ ] 14-day revocation: free only if `first_opened_at` is null and within window
  - [ ] Authoring add-on subscription (€250/year) gating custom course creation
  - [ ] Storage overage metering and charging beyond 5 GB
  - [ ] Credit notes for invoice corrections (invoices themselves are already immutable once issued, enforced by the model/Action from the invoice-generation sub-task above)
  - [ ] VAT handling incl. reverse charge / VAT ID validation
  - [ ] Dunning: failed payment retries, suspension rules
  - [ ] Reseller billing dashboard: current period, breakdown per klant, history
- [ ] Exhaustive unit tests on every pricing calculation

### GDPR

- [ ] Data export per data subject: JSON + human-readable
- [ ] Erasure with anonymization strategy (preserve invoices/audit as legally required)
- [ ] Retention policies per data type, enforced by scheduled job, configurable per reseller
- [ ] Cookie consent (functional-only default, no non-essential without consent)
- [ ] Per-reseller DPA acceptance and record
- [ ] `docs/subprocessors.md` maintained and surfaced in-app
- [ ] Processing register documentation
- [ ] Breach response runbook in `docs/`
- [ ] Confirm PII never reaches application logs or error reports

---

## Phase 9 — Whitelabel Level 2 (block-based pages)

- [ ] Fixed page templates: home, course overview, login, about, contact
- [ ] Configurable content blocks: hero, text, course grid, testimonial, CTA, FAQ, logo strip
- [ ] Reorder blocks in a list (not free canvas), toggle visibility
- [ ] Per-block content editing with translations
- [ ] Preview before publish, draft/published page state
- [ ] Navigation menu builder
- [ ] Per-page SEO fields
- [ ] Confirm Level 3 free-canvas dragging remains out of scope

---

## Phase 10 — AI assisted authoring

- [ ] Provider abstraction behind an interface (no direct vendor calls in domain code)
- [ ] Generate course outline from a topic/brief
- [ ] Generate lesson draft from an outline node
- [ ] Generate draft questions from existing lesson content
- [ ] Suggest distractors for multiple choice
- [ ] Translate existing content into an enabled locale (draft, human-reviewed)
- [ ] Rewrite/simplify text to a target reading level
- [ ] **Everything lands as draft. Nothing auto-publishes. Human review mandatory**
- [ ] Clear AI-generated flag on content, visible to the author
- [ ] Usage metering and per-reseller quota
- [ ] Cost controls and rate limits
- [ ] Prompt-injection hardening on user-supplied source material

---

## Phase 11 — Standards & integrations

- [ ] xAPI statement emission to a configurable external LRS (do this first — cheapest, real value)
- [ ] LTI 1.3 / Advantage as Tool: OIDC launch, JWKS, deep linking, names & roles, grade passback (AGS)
- [ ] LTI platform registration UI per reseller
- [ ] SCORM 1.2 / 2004 package import: upload, unzip, manifest parse, player, CMI runtime, progress mapping
- [ ] SSO: SAML 2.0 and/or OIDC per reseller
- [ ] SCIM user provisioning (only if a customer requires it)
- [ ] Webhooks out for key events (assignment, completion, certificate)
- [ ] Public API — **only when a real consumer exists**

---

## Ongoing (not a phase — do continuously)

- [ ] Keep Larastan baseline shrinking, never growing
- [ ] Keep coverage above 85%
- [ ] Accessibility audit each phase (WCAG 2.1 AA target)
- [ ] Performance: index review, N+1 checks, slow query log
- [ ] Dependency updates + security advisories
- [ ] Keep `docs/` current (architecture decisions, runbooks, subprocessors)
